[
  {"id":"micro-0001","question":"What is a microservices architecture, and what problems does it solve compared to a monolith?","type":"conceptual","difficulty":2,"roles":["backend","system_design"],"topics":["microservices","system-design"],"tech_keywords":["microservices"],"companies_common":["Amazon","Netflix","Uber"],"source_style":"common-interview","reference_solution":{"key_points":["Microservices split an application into small, independently deployable services, each owning a bounded piece of business functionality and its own data","Solves: independent deployment/scaling per service, teams can work autonomously, failure isolation (one service crashing doesn't take down everything)","Trade-off: adds operational complexity — network calls replace function calls, distributed transactions, service discovery, monitoring across many services","Should mention it's not free — a monolith is simpler to develop/debug/deploy for small teams/early-stage products"],"sample_answer":"Microservices architecture splits an application into small, independently deployable services, each owning a specific piece of business capability and typically its own database. This solves problems monoliths have at scale: teams can deploy and scale services independently, a failure in one service doesn't necessarily crash the whole system, and different services can use different tech stacks suited to their needs. The trade-off is significant added complexity — what used to be an in-process function call becomes a network call that can fail, you need service discovery, distributed tracing, and handling for partial failures and data consistency across services. For a small team or early-stage product, a monolith is usually the better starting point."},"follow_up_hint":"Ask when they'd choose to start with a monolith versus microservices for a new product."},
  {"id":"micro-0002","question":"How do microservices typically communicate with each other, and what are the trade-offs between synchronous and asynchronous communication?","type":"trade_off_questions","difficulty":2,"roles":["backend","system_design"],"topics":["microservices"],"tech_keywords":["microservices","rest","message-queue"],"companies_common":["Amazon","Uber"],"source_style":"common-interview","reference_solution":{"key_points":["Synchronous: REST/gRPC calls — caller waits for a response; simple to reason about but couples services' availability together","Asynchronous: message queues/event streams (Kafka, RabbitMQ, SQS) — caller doesn't block, better resilience to downstream slowness/outages","Sync is easier for request/response flows needing an immediate answer; async fits background processing, fan-out, or decoupled workflows","Mentions the cascading-failure risk of long synchronous call chains, mitigated by timeouts/circuit breakers"],"sample_answer":"Services can communicate synchronously via REST or gRPC, where the caller blocks waiting for a response, or asynchronously via a message broker like Kafka, RabbitMQ, or SQS, where the caller publishes an event and moves on. Synchronous calls are simpler to reason about and fit flows that need an immediate answer, but they couple the availability of both services together — if the callee is slow, the caller is stuck waiting, and long synchronous chains can cause cascading failures without timeouts and circuit breakers. Asynchronous messaging decouples services in time, is more resilient to a downstream outage since messages just queue up, and suits background work or fan-out to multiple consumers, but adds complexity around eventual consistency and debugging."},"follow_up_hint":"Ask how they'd prevent a slow downstream service from taking down the entire call chain (circuit breaker pattern)."},
  {"id":"micro-0003","question":"What is service discovery, and why is it needed in a microservices architecture?","type":"conceptual","difficulty":2,"roles":["backend","devops"],"topics":["microservices"],"tech_keywords":["microservices","service-discovery"],"companies_common":["Netflix","Amazon"],"source_style":"common-interview","reference_solution":{"key_points":["In a dynamic environment, service instances' IPs/ports change constantly (scaling, restarts, deployments) so hardcoding addresses doesn't work","Service discovery maintains a registry of currently-available service instances and their locations","Client-side discovery: caller queries the registry directly and picks an instance (e.g. Netflix Eureka)","Server-side discovery: a load balancer/router queries the registry on the caller's behalf (e.g. Kubernetes Service, AWS ALB)"],"sample_answer":"In a microservices deployment, instances of a service are constantly being created, destroyed, or rescheduled — by an autoscaler or a deployment — so their network addresses change all the time, making hardcoded IPs unworkable. Service discovery solves this with a registry that tracks currently healthy instances and their addresses; services register themselves on startup (or a health check confirms they're up) and consumers query the registry to find where to send a request. This can be client-side, where the caller itself queries the registry and load-balances (like Netflix Eureka), or server-side, where a proxy or load balancer does that lookup on the caller's behalf, which is how Kubernetes Services and cloud load balancers work."},"follow_up_hint":"Ask how Kubernetes implements service discovery under the hood."},
  {"id":"micro-0004","question":"Explain the Saga pattern for handling distributed transactions across microservices.","type":"conceptual","difficulty":4,"roles":["backend","system_design"],"topics":["microservices","distributed-systems"],"tech_keywords":["microservices","saga"],"companies_common":["Amazon","Uber","Netflix"],"source_style":"faang-common","reference_solution":{"key_points":["Distributed transactions across services can't use a single ACID transaction/2PC easily at scale, since it requires locking resources across services and blocks on failures","Saga breaks a multi-step business transaction into a sequence of local transactions, each with a corresponding compensating action to undo it if a later step fails","Choreography: each service publishes events that trigger the next step, no central coordinator (more decoupled, harder to trace)","Orchestration: a central saga orchestrator explicitly tells each service what to do next and calls compensations on failure (easier to reason about, adds a coordinator dependency)"],"sample_answer":"A Saga breaks a business transaction spanning multiple services — like 'place order, reserve inventory, charge payment, schedule shipping' — into a sequence of local transactions, each service committing its own step independently, since a traditional distributed ACID transaction or two-phase commit doesn't scale well and creates tight coupling and blocking across services. If a later step fails, the saga runs compensating actions to undo the completed steps — e.g. refunding a payment if shipping scheduling fails. There are two coordination styles: choreography, where each service publishes an event that triggers the next service reactively with no central coordinator, and orchestration, where a central saga orchestrator explicitly directs each step and invokes compensations on failure — orchestration is easier to trace and reason about but introduces a coordinating component."},"follow_up_hint":"Ask them to design the saga and compensating actions for an e-commerce checkout flow."},
  {"id":"micro-0005","question":"What is an API Gateway, and what responsibilities does it typically handle in a microservices system?","type":"conceptual","difficulty":2,"roles":["backend","system_design"],"topics":["microservices","api-design"],"tech_keywords":["api-gateway"],"companies_common":["Amazon","Netflix"],"source_style":"common-interview","reference_solution":{"key_points":["An API Gateway is a single entry point that routes external client requests to the appropriate backend microservice","Common responsibilities: authentication/authorization, rate limiting, request routing, response aggregation from multiple services, SSL termination, logging/metrics","Shields clients from needing to know internal service topology; lets internal services change without breaking external clients","Can become a bottleneck or single point of failure if not scaled/made highly available itself"],"sample_answer":"An API Gateway is the single entry point clients talk to, which then routes each request to the appropriate internal microservice, so external clients don't need to know the internal service topology. It commonly handles cross-cutting concerns centrally — authentication and authorization, rate limiting, SSL termination, request logging/metrics, and sometimes aggregating responses from multiple backend services into one response for the client. The trade-off is that it becomes a critical piece of infrastructure — if it's not made highly available and horizontally scalable itself, it becomes both a bottleneck and a single point of failure for the whole system."},"follow_up_hint":"Ask how they'd prevent the gateway itself from becoming a single point of failure."},
  {"id":"micro-0006","question":"How would you handle data consistency when each microservice has its own database (database-per-service pattern)?","type":"trade_off_questions","difficulty":4,"roles":["backend","system_design"],"topics":["microservices","distributed-systems"],"tech_keywords":["microservices","eventual-consistency"],"companies_common":["Amazon","Uber"],"source_style":"faang-common","reference_solution":{"key_points":["Database-per-service prevents services from being coupled at the schema level, but rules out simple cross-service SQL joins/transactions","Options: eventual consistency via events (a service publishes a change event, others update their own local copy/materialized view asynchronously)","CQRS-style read models can be built by consuming events from multiple services to serve queries needing cross-service data","Sagas (see above) handle multi-service writes that must all succeed or all compensate","Explains the trade-off: strong consistency is sacrificed for service autonomy and independent scaling/deployment"],"sample_answer":"With a database per service, you give up cross-service joins and single-transaction guarantees in exchange for service autonomy. The standard approach is eventual consistency: when a service's data changes, it publishes an event, and other services that need that data consume the event and update their own local copy or materialized view asynchronously — so there's a brief window of inconsistency, but the system converges. For read-heavy scenarios needing data across services, you can build a dedicated read model (CQRS-style) that subscribes to events from multiple services. For multi-service writes that all need to succeed together, you'd use a Saga with compensating actions rather than a two-phase commit. The core trade-off is accepting eventual consistency and added complexity in exchange for services not being tightly coupled at the database level."},"follow_up_hint":"Ask them to explain a concrete scenario where stale eventually-consistent data could cause a user-visible problem, and how they'd mitigate it."},
  {"id":"micro-0007","question":"What is the strangler fig pattern, and when would you use it?","type":"scenario_based","difficulty":3,"roles":["backend","system_design"],"topics":["microservices"],"tech_keywords":["microservices","legacy-migration"],"companies_common":["Amazon","Microsoft"],"source_style":"common-interview","reference_solution":{"key_points":["A strategy for incrementally migrating a monolith to microservices (or replacing any legacy system) without a risky big-bang rewrite","New functionality is built as separate services; a routing layer/facade gradually redirects specific requests from the old system to the new services","Over time, more of the monolith's responsibilities are 'strangled' (replaced), until the old system can be fully retired","Advantage: continuous delivery of value, lower risk than a full rewrite, ability to roll back a single migrated piece if it has issues"],"sample_answer":"The strangler fig pattern is a way to incrementally migrate a legacy monolith to microservices (or replace any legacy system) without a risky big-bang rewrite. You put a routing layer or facade in front of the existing system, and build new functionality as separate services behind that facade; the router directs specific requests to the new services while everything else still goes to the old monolith. Over time, more responsibilities get pulled out into new services — 'strangling' the old system piece by piece — until eventually the old system handles little or nothing and can be decommissioned. This is preferable to a full rewrite because it delivers value continuously, keeps risk contained to one migrated piece at a time, and lets you roll back a single migration if it causes problems."},"follow_up_hint":"Ask how they'd decide which piece of the monolith to strangle first."},
  {"id":"micro-0008","question":"Explain what a circuit breaker pattern does and why it's important in microservices.","type":"conceptual","difficulty":3,"roles":["backend","system_design"],"topics":["microservices"],"tech_keywords":["circuit-breaker","resilience"],"companies_common":["Netflix","Amazon"],"source_style":"faang-common","reference_solution":{"key_points":["A circuit breaker monitors calls to a downstream service and 'trips open' after repeated failures, immediately failing subsequent calls instead of letting them wait/time out","This prevents cascading failures — a struggling downstream service isn't further overwhelmed, and the caller isn't stuck waiting on doomed requests, freeing up its own threads/resources","After a cooldown period, the breaker moves to 'half-open' and allows a trial request through to check if the downstream service has recovered","Closed → Open → Half-open → Closed state machine (standard explanation, e.g. from Netflix Hystrix/resilience4j)"],"sample_answer":"A circuit breaker wraps calls to a downstream service and tracks failure rates; if failures exceed a threshold, it 'trips open' and immediately fails subsequent calls for a cooldown period instead of letting them time out slowly. This matters because it prevents cascading failures — without it, a struggling downstream service gets hammered with more requests it can't handle, while the calling service's own threads/connections get tied up waiting on doomed calls, which can bring the caller down too. After the cooldown, the breaker goes 'half-open' and lets a trial request through; if it succeeds, the breaker closes and normal traffic resumes, otherwise it reopens. This closed-open-half-open cycle is the standard pattern implemented by libraries like Netflix Hystrix or resilience4j."},"follow_up_hint":"Ask what a good fallback behavior looks like when the circuit is open (e.g. cached data, degraded response)."}
]

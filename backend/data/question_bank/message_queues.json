[
  {"id":"mq-0001","question":"What problem do message queues solve in a distributed system?","type":"conceptual","difficulty":2,"roles":["backend","system_design"],"topics":["message-queues","distributed-systems"],"tech_keywords":["kafka","rabbitmq","sqs"],"companies_common":["Amazon","Uber"],"source_style":"common-interview","reference_solution":{"key_points":["Decouples producers and consumers in time — the producer doesn't need the consumer to be available/fast at the moment of sending","Buffers load spikes: a burst of incoming work is queued instead of overwhelming downstream services directly","Enables asynchronous processing and fan-out to multiple consumers","Improves resilience: if a consumer crashes, unprocessed messages remain in the queue instead of being lost"],"sample_answer":"Message queues decouple producers and consumers in time — a producer can publish a message without the consumer needing to be online or fast at that exact moment, since the queue holds it until it's processed. This is great for smoothing out load spikes, since a burst of requests just queues up rather than overwhelming a downstream service directly, and it enables asynchronous background processing and fan-out to multiple independent consumers. It also improves resilience: if a consumer crashes mid-processing, the message can be redelivered rather than lost, unlike a synchronous call that just fails."},"follow_up_hint":"Ask what happens if a consumer crashes after reading a message but before finishing processing it — how do you avoid losing that message?"},
  {"id":"mq-0002","question":"Explain the difference between a message queue (like RabbitMQ/SQS) and an event streaming platform (like Kafka).","type":"conceptual","difficulty":3,"roles":["backend","system_design"],"topics":["message-queues"],"tech_keywords":["kafka","rabbitmq"],"companies_common":["Uber","LinkedIn","Amazon"],"source_style":"faang-common","reference_solution":{"key_points":["Traditional queue (RabbitMQ/SQS): a message is typically removed/invisible once consumed by one consumer — modeled as a work queue for task distribution","Kafka: messages (events) are appended to a durable, ordered log and retained for a configurable period; multiple independent consumer groups can each read the full stream at their own pace, and messages aren't deleted on read","Kafka partitions provide ordering within a partition and horizontal scalability; consumer groups enable parallel consumption","Choose a queue for simple task/work distribution; choose Kafka when you need replay, multiple independent subscribers to the same event stream, or high-throughput ordered event logs"],"sample_answer":"A traditional message queue like RabbitMQ or SQS is built around distributing discrete tasks to workers — a message is generally consumed and removed by one consumer. Kafka is an event streaming/log platform: messages are appended to a durable, ordered, partitioned log and retained for a configurable time regardless of whether they've been read, so multiple independent consumer groups can each read the entire stream at their own pace, and you can replay old events. Kafka partitions give ordering guarantees within a partition and let you scale consumption horizontally via consumer groups. I'd reach for a simple queue for straightforward task distribution to workers, and Kafka when I need multiple subscribers to the same events, replay capability, or very high-throughput ordered event logs, like for event sourcing or analytics pipelines."},"follow_up_hint":"Ask how Kafka guarantees ordering, and whether that guarantee holds across partitions or just within one."},
  {"id":"mq-0003","question":"What is the difference between at-most-once, at-least-once, and exactly-once message delivery semantics?","type":"conceptual","difficulty":3,"roles":["backend","system_design"],"topics":["message-queues","distributed-systems"],"tech_keywords":["kafka","idempotency"],"companies_common":["Amazon","Uber"],"source_style":"faang-common","reference_solution":{"key_points":["At-most-once: message is delivered zero or one time — can be lost, never duplicated (fire-and-forget)","At-least-once: message is guaranteed delivered one or more times — never lost, but consumer must handle duplicates (most common in practice)","Exactly-once: message delivered and processed exactly one time — very hard to guarantee end-to-end, usually achieved via idempotent processing + deduplication rather than a true distributed guarantee","Explains that in practice, most systems implement at-least-once delivery + idempotent consumers to effectively achieve exactly-once processing outcomes"],"sample_answer":"At-most-once means a message might be delivered once or not at all — it can be lost but is never duplicated, which is fine for non-critical data like some metrics. At-least-once means the message is guaranteed to arrive, but might arrive more than once, so the consumer must be able to handle duplicates — this is the most common and practical choice. True exactly-once delivery is extremely hard to guarantee across a network in the general case; in practice, systems achieve an exactly-once processing outcome by combining at-least-once delivery with an idempotent consumer — for example, tracking processed message IDs so reprocessing a duplicate is a safe no-op."},"follow_up_hint":"Ask them to design an idempotent consumer for a payment-processing message that could be delivered twice."},
  {"id":"mq-0004","question":"What is a dead-letter queue, and why would you use one?","type":"practical_implementation","difficulty":2,"roles":["backend"],"topics":["message-queues"],"tech_keywords":["dead-letter-queue"],"companies_common":["Amazon"],"source_style":"common-interview","reference_solution":{"key_points":["A dead-letter queue (DLQ) holds messages that repeatedly fail processing after a set number of retry attempts","Prevents a single 'poison pill' message from blocking the queue or being retried forever, consuming resources","Lets engineers inspect and debug failed messages separately, and potentially replay them after fixing the root cause","Without a DLQ, a malformed message could be retried indefinitely, or silently dropped, either of which is bad"],"sample_answer":"A dead-letter queue holds messages that have failed processing repeatedly, after exceeding a configured retry limit, instead of retrying them forever or dropping them silently. This prevents a single malformed or 'poison pill' message from blocking the queue or endlessly consuming worker resources on retries. It also gives engineers a place to inspect exactly which messages failed and why, and to replay them once the underlying bug or data issue is fixed, rather than losing that data entirely."},"follow_up_hint":"Ask how they'd set an appropriate retry count and backoff strategy before sending a message to the DLQ."},
  {"id":"mq-0005","question":"How does Kafka achieve high throughput and horizontal scalability?","type":"conceptual","difficulty":3,"roles":["backend","system_design"],"topics":["message-queues"],"tech_keywords":["kafka"],"companies_common":["LinkedIn","Uber"],"source_style":"faang-common","reference_solution":{"key_points":["Topics are split into partitions distributed across brokers, allowing parallel reads/writes across machines","Sequential disk writes (append-only log) are much faster than random I/O, and Kafka relies on OS page cache rather than heavy in-process buffering","Consumer groups let multiple consumer instances each own a subset of partitions, parallelizing consumption","Producers can batch and compress messages to reduce network overhead"],"sample_answer":"Kafka scales by splitting each topic into multiple partitions that are distributed across different broker machines, so reads and writes for the same topic can happen in parallel across the cluster. Within a partition, Kafka writes are append-only sequential disk writes, which are far faster than random I/O, and it leans heavily on the OS page cache for reads instead of managing its own in-memory buffer, keeping things efficient. On the consumption side, a consumer group can have multiple instances, each assigned a distinct subset of partitions, so consumption also parallelizes. Producers additionally batch and compress messages before sending, reducing network overhead per message."},"follow_up_hint":"Ask what happens to consumption parallelism if you have more consumers in a group than partitions in the topic."},
  {"id":"mq-0006","question":"What is backpressure in a message-driven system, and how would you handle a slow consumer?","type":"scenario_based","difficulty":3,"roles":["backend","system_design"],"topics":["message-queues"],"tech_keywords":["backpressure"],"companies_common":["Amazon","Uber"],"source_style":"faang-common","reference_solution":{"key_points":["Backpressure is a signal/mechanism that slows down or limits producers when consumers can't keep up, preventing unbounded queue growth","Without it, a slow consumer causes the queue to grow unboundedly, increasing memory use and message latency until something fails","Handling options: rate-limit the producer, add more consumer instances (horizontal scaling) if the workload is parallelizable, buffer with a bounded queue and reject/shed load past a threshold, or apply backpressure signals that propagate upstream (e.g. reactive streams)","Mentions monitoring consumer lag as the key metric to detect this problem early"],"sample_answer":"Backpressure is a mechanism to slow down or limit producers when consumers can't keep up with the incoming rate, preventing the queue from growing unboundedly — without it, a slow consumer just means the queue keeps growing until you run out of memory or the latency becomes unacceptable. To handle a slow consumer, I'd first check if the workload is horizontally scalable and add more consumer instances to increase parallel throughput. If not, I'd consider rate-limiting producers, using a bounded queue that starts shedding or rejecting load past a threshold, or using backpressure-aware streaming (like reactive streams) where the consumer explicitly signals how much it can handle. Monitoring consumer lag — how far behind the consumer is from the latest produced message — is the key metric to catch this early before it becomes a bigger problem."},"follow_up_hint":"Ask how they'd decide whether to scale consumers horizontally versus optimizing the consumer's per-message processing time."}
]

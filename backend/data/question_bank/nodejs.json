[
  {"id":"node-0001","question":"Explain the Node.js event loop and how it enables non-blocking I/O with a single thread.","type":"conceptual","difficulty":3,"roles":["backend"],"topics":["nodejs","javascript"],"tech_keywords":["nodejs","event-loop"],"companies_common":["Amazon","Uber","Netflix"],"source_style":"faang-common","reference_solution":{"key_points":["Node.js runs JS on a single main thread, but delegates I/O operations (file, network, DNS) to the OS/libuv thread pool","The event loop continuously checks for completed async operations and their callbacks, executing them one at a time on the main thread","This lets a single thread handle many concurrent connections without blocking on slow I/O, unlike a thread-per-request model","Explains phases of the event loop briefly (timers, I/O callbacks, poll, check, close) and that CPU-bound synchronous code still blocks the loop since there's only one JS thread"],"sample_answer":"Node.js executes JavaScript on a single main thread, but I/O operations like file reads, network requests, or DNS lookups are delegated to the operating system or libuv's thread pool rather than blocking that main thread. The event loop continuously cycles through phases, checking whether any of those delegated operations have completed, and if so, running their callbacks on the main thread one at a time. This lets a single-threaded process handle thousands of concurrent connections efficiently, since it's not blocked waiting on any one slow I/O operation — but it also means CPU-bound synchronous code, like a heavy computation in a loop, still blocks the entire event loop since there's only one JS thread available to run it."},"follow_up_hint":"Ask how they'd handle a CPU-intensive task in Node without blocking the event loop (worker threads, offloading to another service)."},
  {"id":"node-0002","question":"What is the difference between process.nextTick(), setImmediate(), and setTimeout(fn, 0) in Node.js?","type":"conceptual","difficulty":4,"roles":["backend"],"topics":["nodejs"],"tech_keywords":["nodejs","event-loop"],"companies_common":["Amazon","Uber"],"source_style":"faang-common","reference_solution":{"key_points":["process.nextTick() queues a callback to run immediately after the current operation completes, before the event loop continues to any phase — highest priority","setTimeout(fn, 0) schedules the callback for the timers phase of the event loop, after at least the specified delay (effectively 'as soon as possible' but after I/O in most cases)","setImmediate() schedules a callback to run in the 'check' phase, after I/O callbacks in the current loop iteration","Warns process.nextTick() overuse can starve the event loop, since queued nextTick callbacks are all drained before moving on, which can delay I/O indefinitely if nextTick recursively re-queues itself"],"sample_answer":"process.nextTick() queues a callback to run immediately after the currently executing operation finishes, before the event loop proceeds to any other phase at all — it has the highest priority. setTimeout(fn, 0) schedules the callback for the timers phase, so it runs after at least the given delay once the event loop reaches that phase. setImmediate() schedules the callback for the 'check' phase, which runs after I/O callbacks in the current loop iteration — so inside an I/O callback, setImmediate typically fires before a setTimeout(fn, 0) scheduled at the same point. A caution with process.nextTick is that all queued nextTick callbacks are fully drained before the loop proceeds further, so recursively calling nextTick inside itself can starve the event loop and block I/O indefinitely."},"follow_up_hint":"Ask what would happen if process.nextTick were called recursively without a base case."},
  {"id":"node-0003","question":"How do you handle an unhandled promise rejection in a Node.js application, and why does it matter in production?","type":"practical_implementation","difficulty":2,"roles":["backend"],"topics":["nodejs","javascript"],"tech_keywords":["nodejs","promises"],"companies_common":["Amazon"],"source_style":"common-interview","reference_solution":{"key_points":["An unhandled promise rejection occurs when a rejected promise has no .catch()/try-catch handling it","Listen for process.on('unhandledRejection', handler) to log/alert on these globally as a safety net","In newer Node versions, unhandled rejections can crash the process by default — relying on this as the only handling strategy is risky in production","Best practice: handle rejections at the source (try/catch around await, .catch on promise chains) rather than relying solely on the global handler, which should be a last-resort logging/alerting mechanism"],"sample_answer":"An unhandled rejection happens when a promise rejects and nothing — no .catch() or surrounding try/catch on an await — handles that rejection. You can attach a global process.on('unhandledRejection', handler) as a safety net to log and alert when this happens, since in current Node versions an unhandled rejection can actually crash the process. But that global handler should be a last resort for catching bugs, not the primary error-handling strategy — the real fix is handling rejections at the source, with try/catch around every await and .catch on promise chains, so failures are dealt with explicitly and don't silently become 'unhandled' in the first place."},"follow_up_hint":"Ask what the difference is between an unhandledRejection and an uncaughtException, and how each should be handled."},
  {"id":"node-0004","question":"Explain the difference between CommonJS (require) and ES Modules (import/export) in Node.js.","type":"conceptual","difficulty":2,"roles":["backend"],"topics":["nodejs","javascript"],"tech_keywords":["nodejs","modules"],"companies_common":["Amazon"],"source_style":"common-interview","reference_solution":{"key_points":["CommonJS (require/module.exports) loads modules synchronously and resolves them at runtime; it's Node's original module system","ES Modules (import/export) are statically analyzable — imports/exports are resolved at parse time, which enables tree-shaking/dead-code elimination by bundlers","ESM supports top-level await; CommonJS does not","Interop between the two has caveats (e.g. importing a CommonJS module from ESM, or vice versa) that can trip up dependency resolution"],"sample_answer":"CommonJS, using require() and module.exports, is Node's original module system, resolving and loading modules synchronously at runtime. ES Modules, using import/export syntax, are statically analyzable — the imports and exports are determined at parse time before the code even runs, which is what allows bundlers to do tree-shaking and eliminate unused code. ESM also supports top-level await, which CommonJS doesn't. In practice there are some interop caveats when mixing the two — like a CommonJS module that doesn't have a clean default export being imported into an ESM file — that can occasionally cause confusing bugs in a mixed codebase."},"follow_up_hint":"Ask how they'd migrate an existing CommonJS Node project to ES Modules incrementally."},
  {"id":"node-0005","question":"What is middleware in an Express.js application, and how does the request-response cycle flow through it?","type":"conceptual","difficulty":2,"roles":["backend"],"topics":["nodejs"],"tech_keywords":["nodejs","express"],"companies_common":["Amazon","Infosys"],"source_style":"common-interview","reference_solution":{"key_points":["Middleware functions receive (req, res, next) and execute in the order they're registered, each able to modify req/res or short-circuit the chain by sending a response","Calling next() passes control to the next middleware in the chain; not calling it (without sending a response) leaves the request hanging","Common uses: authentication, logging, body parsing, error handling — cross-cutting concerns applied to many/all routes","Error-handling middleware has a special 4-argument signature (err, req, res, next) and is typically registered last"],"sample_answer":"Middleware in Express are functions with the signature (req, res, next) that run in the order they're registered for a matching request. Each middleware can inspect or modify the request/response, and must either call next() to pass control to the next middleware in the chain, or end the response itself — if it does neither, the request just hangs. This pattern is used for cross-cutting concerns like authentication checks, request logging, parsing the request body, or centralized error handling, without duplicating that logic in every route handler. Error-handling middleware is a special case with a 4-argument signature (err, req, res, next), and is registered last so Express routes any error passed via next(err) to it."},"follow_up_hint":"Ask how error-handling middleware differs in signature and where it needs to be placed in the middleware stack."}
]

[
  {"id":"react-0001","question":"Explain how React's Virtual DOM works and why it improves performance.","type":"conceptual","difficulty":2,"roles":["frontend"],"topics":["react","frontend"],"tech_keywords":["react"],"companies_common":["Meta","Amazon"],"source_style":"common-interview","reference_solution":{"key_points":["Virtual DOM is a lightweight in-memory JS representation of the actual DOM tree","On state change, React builds a new virtual DOM tree and diffs it against the previous one (reconciliation)","Only the actual minimal set of real DOM mutations needed to match the new virtual tree are applied, since real DOM operations are comparatively expensive","Clarifies it's not always faster than manual optimal DOM updates, but makes writing declarative UI code fast enough by default without manual DOM management"],"sample_answer":"The Virtual DOM is a lightweight JavaScript object tree that mirrors the actual DOM structure. When state changes, React builds a new virtual DOM tree and diffs it against the previous version — a process called reconciliation — to compute the minimal set of actual DOM mutations needed. It then applies just those changes to the real DOM, which is comparatively expensive to manipulate directly. This isn't necessarily faster than a hand-optimized direct DOM update would be, but it lets developers write declarative, easy-to-reason-about UI code without manually tracking and minimizing DOM operations themselves."},"follow_up_hint":"Ask what the 'key' prop is used for during list reconciliation and what happens if it's omitted or incorrect."},
  {"id":"react-0002","question":"What is the difference between useMemo and useCallback in React?","type":"conceptual","difficulty":2,"roles":["frontend"],"topics":["react"],"tech_keywords":["react","hooks"],"companies_common":["Meta","Amazon"],"source_style":"common-interview","reference_solution":{"key_points":["useMemo memoizes a computed VALUE, recomputing it only when its dependencies change","useCallback memoizes a FUNCTION reference itself, so the same function identity is preserved across renders unless dependencies change","Both exist primarily to avoid unnecessary recomputation or to prevent unnecessary re-renders of memoized child components (React.memo) that would otherwise see a 'new' prop reference every render","Cautions against overusing them — they have their own overhead and are only worth it when the computation is expensive or re-renders are provably a problem"],"sample_answer":"useMemo memoizes a computed value, only recalculating it when one of its dependencies changes, which is useful for expensive computations. useCallback memoizes a function's reference itself, so the function identity stays stable across re-renders unless its dependencies change — this mainly matters when passing callbacks to a child wrapped in React.memo, since otherwise a new function reference every render would defeat that memoization. Both add their own small overhead, so I'd only reach for them when profiling shows an actual expensive recomputation or unnecessary re-render, not by default everywhere."},"follow_up_hint":"Ask them to give an example where using useCallback without React.memo on the child provides no benefit."},
  {"id":"react-0003","question":"Explain the React reconciliation process and how the 'key' prop affects it in lists.","type":"conceptual","difficulty":3,"roles":["frontend"],"topics":["react"],"tech_keywords":["react"],"companies_common":["Meta"],"source_style":"faang-common","reference_solution":{"key_points":["React's diffing algorithm compares elements by type and position by default when reconciling children","For lists, without stable keys, React matches old and new children by index, which can misattribute state to the wrong item when items are reordered/inserted/removed","A stable, unique 'key' lets React correctly track which item is which across renders, even if the order changes, preserving correct component state and minimizing DOM operations","Using array index as key is problematic specifically when list order/composition can change, since the index doesn't identify the item"],"sample_answer":"When reconciling a list of children, React needs to match old elements to new ones to decide what to update, add, or remove. Without a stable key, it falls back to matching by index position, which can cause bugs when the list is reordered or an item is inserted in the middle — component state can end up attached to the wrong logical item, since React thinks 'the thing at index 2' is the same element even though the actual data there changed. A unique, stable key — usually a database ID — lets React correctly identify each item across renders regardless of reordering, so it preserves the right state and only does the minimal necessary DOM operations. Using the array index as a key defeats this whenever the list's order or membership can change."},"follow_up_hint":"Ask them to describe a concrete bug caused by using array index as a key in a reorderable to-do list."},
  {"id":"react-0004","question":"What are React Server Components, and how do they differ from traditional client-rendered components?","type":"conceptual","difficulty":4,"roles":["frontend"],"topics":["react"],"tech_keywords":["react","ssr"],"companies_common":["Meta","Vercel"],"source_style":"faang-common","reference_solution":{"key_points":["Server Components render entirely on the server and send serialized output (not JS bundle code) to the client — no client-side JS is shipped for them","Reduces client bundle size since server-only logic/dependencies never reach the browser","Can directly access server resources (databases, filesystem) without an API layer, since they never run in the browser","Cannot use browser-only APIs, state, or effects (no useState/useEffect) since they don't run/re-render on the client; interactive parts still need Client Components"],"sample_answer":"React Server Components render exclusively on the server, and instead of shipping the component's JavaScript to the browser, only its rendered output/serialized representation is sent to the client — so their code and dependencies never bloat the client bundle. This also lets them directly access server-only resources like a database or filesystem without needing a separate API endpoint. The trade-off is they can't use browser-only features, state, or effects like useState or useEffect, since they don't run or re-render in the browser at all — any interactive UI still needs to be a traditional Client Component, and Server and Client Components are composed together in the same tree."},"follow_up_hint":"Ask how data fetched in a Server Component gets passed down to an interactive Client Component."},
  {"id":"react-0005","question":"Explain what happens during React's render phase versus commit phase.","type":"conceptual","difficulty":3,"roles":["frontend"],"topics":["react"],"tech_keywords":["react"],"companies_common":["Meta"],"source_style":"faang-common","reference_solution":{"key_points":["Render phase: React calls component functions to figure out what the UI should look like, building the new virtual DOM tree and diffing it — this phase is pure and can be paused/aborted/restarted (used by concurrent features)","Commit phase: React actually applies the computed DOM mutations, runs lifecycle methods like componentDidMount/useLayoutEffect, and this phase is synchronous and can't be interrupted","Explains why side effects shouldn't happen during render (calling components multiple times, or aborting a render, would run the side effect incorrectly) — that's why useEffect exists to run after commit"],"sample_answer":"During the render phase, React calls your component functions to compute what the new UI tree should look like, and diffs it against the previous tree — this phase is meant to be pure with no side effects, since React may call it more than once, pause it, or throw away the result entirely under concurrent rendering features. The commit phase is where React actually applies the computed DOM mutations and runs effects tied to DOM changes, like componentDidMount or useLayoutEffect — this phase is synchronous and can't be interrupted. This distinction is exactly why side effects belong in useEffect rather than directly in the component body — putting a side effect in render risks it running multiple times or running for a render that gets thrown away."},"follow_up_hint":"Ask why useLayoutEffect runs synchronously during commit while useEffect runs asynchronously after paint."},
  {"id":"react-0006","question":"What is prop drilling, and what are the common solutions to avoid it?","type":"trade_off_questions","difficulty":2,"roles":["frontend"],"topics":["react"],"tech_keywords":["react","state-management"],"companies_common":["Amazon","Meta"],"source_style":"common-interview","reference_solution":{"key_points":["Prop drilling is passing a prop through multiple intermediate components that don't use it themselves, just to get it to a deeply nested descendant","Solutions: React Context API for moderate cross-cutting data (theme, auth user) without prop chains","Global state management libraries (Redux, Zustand, Jotai) for more complex/frequently-updated shared state with better devtools and performance controls","Component composition (passing children/render props) can also avoid drilling by restructuring the component tree instead of threading props through it"],"sample_answer":"Prop drilling is when a prop has to be passed through several intermediate components that don't actually use it, purely to reach a deeply nested child that does. For moderate cases like theme or the logged-in user, React's Context API avoids this by letting a deeply nested component read a value directly without every level in between passing it along. For more complex or frequently-changing shared state, a dedicated state management library like Redux, Zustand, or Jotai often works better, since Context re-renders every consumer on any change and lacks some devtools/performance features those libraries provide. Restructuring the component tree with composition — passing a fully-built child element down as a prop instead of raw data — can also sidestep the need to drill data through layers at all."},"follow_up_hint":"Ask about a performance downside of using Context for very frequently changing state."}
]

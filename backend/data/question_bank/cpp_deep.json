[
  {"id":"cppd-0001","question":"Explain RAII (Resource Acquisition Is Initialization) in C++ and why it's central to modern C++ resource management.","type":"conceptual","difficulty":3,"roles":["backend","general_sde"],"topics":["cpp"],"tech_keywords":["cpp","raii"],"companies_common":["Google","Microsoft","Amazon"],"source_style":"faang-common","reference_solution":{"key_points":["RAII ties a resource's lifetime (memory, file handle, lock, socket) to an object's lifetime — acquired in the constructor, released in the destructor","When the object goes out of scope, its destructor runs automatically (even during stack unwinding from an exception), guaranteeing cleanup without needing explicit manual release calls","This is what smart pointers (unique_ptr, shared_ptr), std::lock_guard, and std::fstream are built on — no manual delete/unlock/close needed in normal control flow","Prevents resource leaks that plague manual resource management, especially in the presence of early returns or exceptions that could otherwise skip a cleanup call"],"sample_answer":"RAII ties the lifetime of a resource — memory, a file handle, a mutex lock, a socket — directly to the lifetime of an object: the resource is acquired in the object's constructor and released in its destructor. Because C++ guarantees destructors run automatically when an object goes out of scope, including during stack unwinding triggered by an exception, this guarantees cleanup happens reliably without needing an explicit manual release call scattered through every possible exit path of a function. This is exactly what smart pointers like unique_ptr and shared_ptr, std::lock_guard for mutexes, and std::fstream for files are built on — you just let the object go out of scope and cleanup happens automatically. It's central to modern C++ because it prevents the resource leaks that plague manual management with raw new/delete or manual lock/unlock, especially when a function has multiple early returns or can throw partway through."},"follow_up_hint":"Ask them to explain what would go wrong with manual delete-based cleanup if an exception was thrown between the allocation and the delete call."},
  {"id":"cppd-0002","question":"What is the difference between a unique_ptr and a shared_ptr in modern C++?","type":"conceptual","difficulty":3,"roles":["backend"],"topics":["cpp"],"tech_keywords":["cpp","smart-pointers"],"companies_common":["Google","Microsoft"],"source_style":"faang-common","reference_solution":{"key_points":["unique_ptr represents exclusive ownership of a resource — it can't be copied (only moved), so there's always exactly one owner responsible for eventually deleting it","shared_ptr uses reference counting to allow multiple owners; the resource is deleted only once the last shared_ptr referencing it is destroyed","unique_ptr has essentially zero overhead compared to a raw pointer; shared_ptr has overhead from atomic reference counting (thread-safe increments/decrements)","A shared_ptr cycle (two objects holding shared_ptrs to each other) leaks memory, since the reference count never reaches zero — weak_ptr is used to break such cycles"],"sample_answer":"unique_ptr represents exclusive ownership of a resource — it can't be copied, only moved, so there's always exactly one owner responsible for eventually freeing it, and it has essentially zero performance overhead compared to a raw pointer. shared_ptr instead uses atomic reference counting to allow multiple owners of the same resource, deleting it only once the very last shared_ptr referencing it goes out of scope — this flexibility comes with real overhead from those atomic increment/decrement operations needed for thread safety. One important pitfall with shared_ptr is a reference cycle: if two objects hold shared_ptrs to each other, their reference counts never reach zero even when nothing else references either of them, causing a memory leak — the standard fix is using weak_ptr for one side of that relationship, which observes an object without contributing to its reference count."},"follow_up_hint":"Ask them to identify and fix a shared_ptr cycle in a doubly-linked list or parent-child relationship example."},
  {"id":"cppd-0003","question":"Explain what a virtual function is in C++ and how virtual function dispatch (vtable) works at runtime.","type":"conceptual","difficulty":4,"roles":["backend","general_sde"],"topics":["cpp","oop"],"tech_keywords":["cpp","virtual-functions"],"companies_common":["Microsoft","Google"],"source_style":"faang-common","reference_solution":{"key_points":["A virtual function allows a derived class to override a base class's method, and the correct overridden version is called based on the object's actual runtime type, not the static type of the pointer/reference used to call it","Implemented via a vtable (virtual method table): each class with virtual functions has a table of function pointers, and each object instance stores a hidden pointer to its class's vtable","A call through a base class pointer/reference to a virtual function looks up the actual function to call via the object's vtable pointer at runtime, rather than being resolved at compile time — this is what enables polymorphism","This indirection has a small runtime performance cost (an extra pointer dereference) compared to a non-virtual (statically resolved) function call, which is why virtual functions aren't used by default for every method"],"sample_answer":"A virtual function lets a derived class override a base class method, and crucially, the version that actually gets called is determined by the object's real runtime type, not by the static, compile-time type of the pointer or reference used to call it — this is what enables runtime polymorphism. Under the hood, this is implemented via a vtable: any class that has virtual functions gets a table of function pointers, one per virtual function, and every instance of that class carries a hidden pointer to its class's vtable. When you call a virtual function through a base class pointer, the compiler generates code that looks up the actual function address through that object's vtable pointer at runtime, rather than resolving the call at compile time the way a normal function call would be. This indirection — an extra pointer dereference to look up the vtable, then another to call the function — has a small but real runtime cost, which is why C++ requires you to opt in explicitly with the 'virtual' keyword rather than making every method virtual by default."},"follow_up_hint":"Ask them what happens if a base class destructor isn't marked virtual, and you delete a derived object through a base class pointer."}
]

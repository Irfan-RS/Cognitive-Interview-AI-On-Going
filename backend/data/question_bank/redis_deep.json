[
  {"id":"redis-0001","question":"Why is Redis so fast, and what are the trade-offs of it being primarily an in-memory data store?","type":"trade_off_questions","difficulty":2,"roles":["backend"],"topics":["caching","redis"],"tech_keywords":["redis"],"companies_common":["Amazon","Uber"],"source_style":"common-interview","reference_solution":{"key_points":["Redis stores data primarily in RAM, avoiding disk I/O for reads/writes, which is orders of magnitude faster than disk-based access","Single-threaded execution model for command processing (per core) avoids locking overhead for most operations, and its data structures are optimized for O(1)/O(log n) operations","Trade-off: data is volatile by default if the machine restarts, unless persistence (RDB snapshots and/or AOF logging) is configured, and even then, RDB/AOF add some overhead and don't have the same durability guarantees as a disk-first database","Trade-off: total dataset size is limited by available RAM, which is more expensive per GB than disk, making Redis less suited as a primary store for very large datasets compared to specialized use as a cache or fast auxiliary store"],"sample_answer":"Redis is fast primarily because it stores data in RAM rather than on disk, and RAM access is orders of magnitude faster than disk I/O for both reads and writes. It also uses a largely single-threaded execution model for command processing, which avoids the locking overhead that a multi-threaded system would need to coordinate concurrent access, and its data structures are specifically designed for efficient O(1) or O(log n) operations. The trade-off is that data is volatile by default — if the Redis process or machine restarts without persistence configured, everything in memory is lost — so for anything that needs to survive a restart, you'd configure RDB snapshotting, AOF (append-only file) logging, or both, though even with those enabled, Redis's durability guarantees are still generally weaker than a disk-first database designed around durability first. The other trade-off is that your total dataset size is limited by available RAM, which is significantly more expensive per gigabyte than disk, which is exactly why Redis is typically used as a cache or fast auxiliary data store rather than as the primary system of record for very large datasets."},"follow_up_hint":"Ask them to explain the difference between RDB snapshotting and AOF persistence, and the durability trade-off between them."},
  {"id":"redis-0002","question":"Name a few Redis data structures beyond simple key-value strings, and describe a use case for each.","type":"conceptual","difficulty":2,"roles":["backend"],"topics":["redis"],"tech_keywords":["redis"],"companies_common":["Amazon","Uber"],"source_style":"common-interview","reference_solution":{"key_points":["Lists: ordered collections, useful for a simple queue or a recent-activity feed (push to one end, pop/read from the other)","Sets: unordered unique collections, useful for tracking unique visitors/tags, and support fast set operations (union, intersection) — e.g. finding mutual friends","Sorted Sets (ZSET): elements ordered by an associated score, useful for leaderboards or rate-limiting windows, supporting fast range queries by rank or score","Hashes: field-value pairs under one key, useful for representing an object (like a user profile) compactly, avoiding the overhead of many separate top-level keys for related fields"],"sample_answer":"Beyond simple string keys, Redis Lists are ordered collections, useful for something like a simple task queue or a recent-activity feed, where you push new items onto one end and pop or read from the other. Sets are unordered collections of unique values, useful for tracking things like unique visitor IDs or a set of tags, and they support fast operations like intersection or union — for example, finding mutual followers between two users. Sorted Sets, or ZSETs, keep elements ordered by an associated numeric score, which is great for building a leaderboard, since you get fast range queries by rank or score, or for implementing rate-limiting windows based on timestamps as scores. Hashes store field-value pairs grouped under a single key, which is a natural fit for representing something like a user profile object compactly under one Redis key, rather than needing many separate top-level keys for each individual field."},"follow_up_hint":"Ask them to design a real-time leaderboard using a Redis sorted set, including how they'd get a player's current rank."}
]

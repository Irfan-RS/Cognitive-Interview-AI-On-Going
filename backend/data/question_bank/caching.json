[
  {"id":"cache-0001","question":"What are the common cache eviction policies, and how does LRU work?","type":"conceptual","difficulty":2,"roles":["backend","system_design"],"topics":["caching","system-design"],"tech_keywords":["redis","lru"],"companies_common":["Amazon","Google"],"source_style":"common-interview","reference_solution":{"key_points":["Common policies: LRU (Least Recently Used), LFU (Least Frequently Used), FIFO, random eviction","LRU evicts the item that hasn't been accessed for the longest time, on the assumption recently used items are likely to be used again soon","Typically implemented with a doubly linked list + hash map: hash map for O(1) lookup, linked list to track recency order, moving an accessed item to the front in O(1)","LFU tracks access frequency instead of recency — better when some items are consistently hot regardless of recent access pattern, but more overhead to maintain counts"],"sample_answer":"Common eviction policies include LRU, which evicts whichever item hasn't been accessed for the longest time, LFU, which evicts the least frequently accessed item, and simpler policies like FIFO or random eviction. LRU is typically implemented with a hash map for O(1) key lookup combined with a doubly linked list that tracks access recency — accessing an item moves it to the front of the list in O(1), and eviction removes from the back. LRU works well when recently accessed items are likely to be accessed again soon; LFU can perform better when certain items are consistently 'hot' regardless of how recently they were touched, but requires maintaining frequency counters, adding overhead."},"follow_up_hint":"Ask them to implement an LRU cache with get/put in O(1) time."},
  {"id":"cache-0002","question":"Explain cache-aside, write-through, and write-back caching strategies and their trade-offs.","type":"trade_off_questions","difficulty":3,"roles":["backend","system_design"],"topics":["caching"],"tech_keywords":["redis","caching"],"companies_common":["Amazon","Uber"],"source_style":"faang-common","reference_solution":{"key_points":["Cache-aside (lazy loading): application checks cache first, on miss reads from DB and populates cache; writes go to DB and typically invalidate/update the cache entry — simple, cache only holds requested data","Write-through: writes go to the cache first, which synchronously writes through to the DB — keeps cache and DB consistent, but adds write latency","Write-back (write-behind): writes go to the cache and are asynchronously flushed to the DB later — fastest writes, but risks data loss if the cache fails before flushing","Explains choosing based on read/write ratio and consistency/durability requirements"],"sample_answer":"Cache-aside is the most common pattern: the application checks the cache first, and on a miss, reads from the database and populates the cache for next time; writes go directly to the database, with the cache entry invalidated or updated alongside. It's simple and the cache only ever holds data that's actually been requested. Write-through writes to the cache first, which synchronously also writes to the database, keeping both consistent at the cost of added write latency. Write-back (write-behind) writes only to the cache immediately and asynchronously flushes to the database later, giving the fastest write performance but risking data loss if the cache crashes before the flush happens. I'd pick cache-aside by default for read-heavy workloads, write-through when consistency matters more than write latency, and write-back only when write throughput is critical and some data loss risk is acceptable."},"follow_up_hint":"Ask what happens to cache-aside consistency if two requests race — one reading a miss and populating stale data while another concurrently updates the DB."},
  {"id":"cache-0003","question":"What is cache invalidation, and why is it considered a hard problem?","type":"conceptual","difficulty":3,"roles":["backend","system_design"],"topics":["caching"],"tech_keywords":["caching"],"companies_common":["Amazon","Google"],"source_style":"faang-common","reference_solution":{"key_points":["Cache invalidation means removing/updating stale cached data once the underlying source of truth changes","Hard because: multiple cache layers (browser, CDN, application, DB query cache) may all hold stale copies with different TTLs, and coordinating invalidation across all of them is error-prone","Race conditions: an update to the DB and a cache invalidation aren't atomic, so a concurrent read can populate the cache with stale data right after invalidation","Common mitigations: short TTLs as a safety net, versioned/keyed cache entries (invalidate by changing the key rather than deleting), event-driven invalidation on data change"],"sample_answer":"Cache invalidation means ensuring that once the underlying data changes, any cached copies of the old value get removed or updated so clients don't keep seeing stale data. It's hard because there are often multiple layers of caching — browser cache, CDN, application-level cache, database query cache — each with their own TTL and invalidation logic, and keeping all of them consistent is genuinely difficult. There's also a subtle race condition: a database update and a cache invalidation aren't atomic, so a read that happens to occur in between could re-populate the cache with the stale value right after it was just invalidated. Common mitigations are relying on a reasonably short TTL as a safety net so staleness is bounded, versioning cache keys so an update effectively creates a 'new' key rather than needing to delete the old one, and driving invalidation directly off data-change events rather than time alone."},"follow_up_hint":"Ask them to describe the race condition where a cache is invalidated right before a stale read repopulates it, and how they'd fix it."},
  {"id":"cache-0004","question":"What is a thundering herd problem in caching, and how would you prevent it?","type":"scenario_based","difficulty":4,"roles":["backend","system_design"],"topics":["caching"],"tech_keywords":["caching","redis"],"companies_common":["Amazon","Meta"],"source_style":"faang-common","reference_solution":{"key_points":["Thundering herd: when a popular cache entry expires, many concurrent requests all miss the cache simultaneously and hit the database/backend at once, potentially overwhelming it","Mitigations: locking/mutex so only one request recomputes the value while others wait for it or serve a slightly-stale value","Probabilistic early expiration (recompute slightly before actual TTL, staggered per key) to avoid many keys expiring at the exact same moment","Serve stale-while-revalidate: return the stale cached value immediately while asynchronously refreshing it in the background"],"sample_answer":"Thundering herd happens when a popular cache entry expires and a large number of concurrent requests all miss the cache at the same instant, so they all hit the database or backend simultaneously trying to recompute the same value — potentially overwhelming it right at that moment. A common fix is a lock or mutex per cache key so only one request recomputes the value on a miss, while other concurrent requests either wait briefly for that result or are served the stale value in the meantime. Another approach is stale-while-revalidate: keep serving the slightly-stale cached value immediately while asynchronously refreshing it in the background, so no request ever actually blocks on a slow recompute. Staggering TTLs with some jitter also helps avoid many keys expiring at the exact same moment in the first place."},"follow_up_hint":"Ask how they'd implement the per-key lock so that only one process refreshes the cache while others wait or serve stale data."}
]

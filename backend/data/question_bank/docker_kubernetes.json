[
  {"id":"dk-0001","question":"What is the difference between a Docker container and a virtual machine?","type":"conceptual","difficulty":1,"roles":["devops","backend"],"topics":["docker","devops"],"tech_keywords":["docker"],"companies_common":["Amazon","Infosys"],"source_style":"common-interview","reference_solution":{"key_points":["A VM virtualizes hardware and runs a full separate OS kernel per VM, managed by a hypervisor — heavier resource footprint","A container shares the host OS kernel and isolates processes using kernel features (namespaces, cgroups), making it much lighter weight and faster to start","Containers are more portable (package app + dependencies) but provide weaker isolation than VMs since they share the kernel","Explains typical use: containers for fast, dense application deployment; VMs when you need full OS-level isolation or different kernels/OSes on the same host"],"sample_answer":"A virtual machine virtualizes hardware and runs its own complete guest operating system with its own kernel, managed by a hypervisor — this gives strong isolation but is comparatively heavyweight and slow to boot. A container shares the host machine's OS kernel and achieves isolation through kernel features like namespaces and cgroups, making it much lighter weight, faster to start, and more efficient to run many of on one host. The trade-off is containers provide weaker isolation than VMs since they share a kernel, so if isolation from potentially hostile workloads is critical, VMs (or a VM-based container runtime like gVisor/Firecracker) are safer. In practice, containers are preferred for packaging and deploying applications efficiently, while VMs are used when you need full OS-level isolation or to run different operating systems on the same physical hardware."},"follow_up_hint":"Ask what namespaces and cgroups each specifically provide for container isolation."},
  {"id":"dk-0002","question":"What is a Kubernetes Pod, and why does Kubernetes use Pods instead of scheduling containers directly?","type":"conceptual","difficulty":2,"roles":["devops","backend"],"topics":["kubernetes"],"tech_keywords":["kubernetes"],"companies_common":["Amazon","Google"],"source_style":"common-interview","reference_solution":{"key_points":["A Pod is the smallest deployable unit in Kubernetes — one or more containers that share network namespace (same IP/port space) and storage volumes","Containers within a Pod are always co-scheduled onto the same node and share a lifecycle","Enables the 'sidecar' pattern: a helper container (logging agent, proxy) tightly coupled to the main container, sharing localhost networking and volumes","Explains most Pods run a single container, with multi-container Pods reserved for genuinely tightly-coupled helper processes"],"sample_answer":"A Pod is the smallest deployable unit in Kubernetes, consisting of one or more containers that are always scheduled together onto the same node, sharing the same network namespace — so they can talk to each other over localhost — and optionally shared storage volumes. This grouping enables the sidecar pattern, where a helper container like a logging agent or a proxy runs alongside the main application container, tightly coupled to it and sharing its network and lifecycle. Most Pods just run a single container; multi-container Pods are reserved for cases where two processes genuinely need to be co-located and share resources like that."},"follow_up_hint":"Ask them to describe a real use case for the sidecar pattern, like an Istio service mesh proxy."},
  {"id":"dk-0003","question":"Explain the difference between a Kubernetes Deployment and a StatefulSet.","type":"conceptual","difficulty":3,"roles":["devops"],"topics":["kubernetes"],"tech_keywords":["kubernetes"],"companies_common":["Amazon","Google"],"source_style":"faang-common","reference_solution":{"key_points":["Deployment manages stateless, interchangeable Pod replicas — Pods get arbitrary names/IPs and can be freely replaced/rescheduled in any order","StatefulSet manages Pods that need stable, unique identities (predictable names like pod-0, pod-1) and stable storage (each gets its own persistent volume that survives rescheduling)","StatefulSet also guarantees ordered, sequential deployment/scaling/termination (pod-0 starts before pod-1, etc.), which matters for clustered stateful systems (databases, Kafka, Zookeeper)","Explains when to use which: stateless web servers/APIs → Deployment; databases/clustered systems needing stable identity/storage → StatefulSet"],"sample_answer":"A Deployment manages a set of stateless, interchangeable Pod replicas — any replica can be killed and replaced with a new one with a new name and IP, and there's no ordering guarantee among them, which fits stateless web servers or APIs. A StatefulSet is for workloads that need stable, unique identity and storage — each Pod gets a predictable, sticky name like myapp-0, myapp-1, and its own persistent volume that follows it even if it's rescheduled to a different node, rather than a shared or randomly-assigned one. StatefulSets also start, scale, and terminate Pods in a defined sequential order, which matters for clustered stateful systems like databases, Kafka, or Zookeeper, where node identity and startup order affect cluster formation and data integrity."},"follow_up_hint":"Ask why running a database directly in Kubernetes via StatefulSet is still considered operationally harder than using a managed database service."},
  {"id":"dk-0004","question":"What is a Kubernetes readiness probe versus a liveness probe, and why do you need both?","type":"conceptual","difficulty":2,"roles":["devops"],"topics":["kubernetes"],"tech_keywords":["kubernetes"],"companies_common":["Amazon","Google"],"source_style":"common-interview","reference_solution":{"key_points":["Liveness probe checks if a container is still running/healthy; if it fails repeatedly, Kubernetes restarts the container","Readiness probe checks if a container is ready to accept traffic; if it fails, the Pod is removed from the Service's load-balancing pool (but not restarted)","Distinguishes them: a container can be alive but not ready (e.g. still warming up caches, or temporarily overloaded and wanting to shed traffic) — restarting it in that case would be wrong and disruptive","Without a readiness probe, traffic could be routed to a Pod that isn't actually ready to serve requests, causing errors"],"sample_answer":"A liveness probe checks whether a container is still healthy and running correctly; if it fails repeatedly, Kubernetes kills and restarts the container, since something is presumably stuck or broken. A readiness probe checks whether the container is currently ready to serve traffic; if it fails, Kubernetes just removes that Pod from the Service's load-balanced endpoints without restarting it. You need both because a container can be alive but not ready — for example, still loading a large cache on startup, or temporarily overloaded and wanting to shed incoming traffic — restarting it in that situation would be wrong and just cause more disruption. Without a readiness probe at all, Kubernetes might route live traffic to a Pod that isn't actually able to handle it correctly yet, causing user-visible errors."},"follow_up_hint":"Ask what happens during a rolling deployment if the new Pods' readiness probes never succeed."},
  {"id":"dk-0005","question":"What is a multi-stage Docker build, and why would you use one?","type":"practical_implementation","difficulty":2,"roles":["devops","backend"],"topics":["docker"],"tech_keywords":["docker"],"companies_common":["Amazon"],"source_style":"common-interview","reference_solution":{"key_points":["A multi-stage build uses multiple FROM statements in one Dockerfile, where later stages can selectively copy artifacts from earlier stages","Lets you use a full build toolchain (compilers, dev dependencies) in an early stage, but ship only the compiled output in a minimal final runtime image","Reduces final image size significantly and avoids shipping build tools/source code/secrets used only during compilation into production","Common pattern: a 'builder' stage with full SDK/dependencies, then a final stage FROM a minimal base image (e.g. alpine or distroless) COPYing just the compiled binary/artifacts"],"sample_answer":"A multi-stage Docker build uses multiple FROM statements within a single Dockerfile, where each stage can copy specific files or build artifacts from a previous stage. This lets you use a full build environment — compilers, dev dependencies, build tools — in an early 'builder' stage, but then start a final stage from a minimal runtime base image and copy over only the compiled output, like a binary or built static files, discarding everything else. The benefit is a much smaller and more secure final production image, since it doesn't carry the build toolchain, source code, or any build-time secrets that were only needed to produce the artifact, not to run it."},"follow_up_hint":"Ask them to sketch a multi-stage Dockerfile for a Go or Node.js app that builds in one stage and runs from a minimal image in the next."}
]

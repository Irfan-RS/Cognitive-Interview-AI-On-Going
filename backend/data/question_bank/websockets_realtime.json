[
  {"id":"ws-0001","question":"Explain how WebSockets differ from traditional HTTP polling for real-time features.","type":"trade_off_questions","difficulty":2,"roles":["backend","frontend"],"topics":["networking","real-time"],"tech_keywords":["websockets"],"companies_common":["Amazon","Uber"],"source_style":"common-interview","reference_solution":{"key_points":["HTTP polling: client repeatedly sends requests at an interval asking 'anything new?' — simple, but wastes requests when there's nothing new, and adds latency up to the polling interval","Long polling: client sends a request that the server holds open until there's new data (or a timeout), reducing wasted requests compared to fixed-interval polling, but still involves repeated connection setup","WebSockets: establish a single persistent, full-duplex connection after an initial HTTP handshake, allowing the server to push data to the client instantly without the client needing to ask again","WebSockets are the better choice for frequent, low-latency bidirectional communication (chat, live dashboards, multiplayer); simple polling can be fine for infrequent updates where implementation simplicity matters more"],"sample_answer":"With basic HTTP polling, the client repeatedly sends requests at a fixed interval asking if there's anything new — it's simple to implement, but wastes requests when nothing has changed, and introduces latency up to the length of the polling interval, since new data has to wait for the next poll to be picked up. Long polling improves on this: the client sends a request the server holds open until there's actually new data to return, or a timeout is hit, reducing wasted round trips compared to fixed-interval polling, but still involves setting up a new connection repeatedly. WebSockets solve this more fundamentally by upgrading an initial HTTP connection into a single persistent, full-duplex connection, letting the server push data to the client the instant it's available, with no need for the client to ask again at all. I'd reach for WebSockets for frequent, low-latency, bidirectional use cases like chat apps, live dashboards, or multiplayer games, and stick with simple polling only for infrequent updates where the implementation simplicity outweighs the inefficiency."},"follow_up_hint":"Ask them how they'd handle a WebSocket connection dropping unexpectedly and needing to reconnect without losing messages."},
  {"id":"ws-0002","question":"How would you design a real-time notification system that scales to millions of concurrently connected users?","type":"scenario_based","difficulty":4,"roles":["backend","system_design"],"topics":["system-design","real-time"],"tech_keywords":["websockets","pub-sub"],"companies_common":["Uber","Meta","Amazon"],"source_style":"faang-common","reference_solution":{"key_points":["A single server can only hold a limited number of concurrent WebSocket connections, so you need many 'connection/gateway' server instances, each holding a subset of users' connections","When an event needs to reach a specific user, the originating service doesn't know which gateway server holds that user's connection — needs a mechanism to route the message there","Solution: a pub/sub layer (Redis Pub/Sub, Kafka) that all gateway servers subscribe to; the originating service publishes the notification, and only the gateway instance holding that user's connection actually delivers it","Also needs a presence/connection registry (mapping user ID to which gateway instance/connection they're on) if you want to route directly rather than broadcast to all gateways and have each check relevance"],"sample_answer":"A single server can only hold so many concurrent WebSocket connections open at once, so I'd run many stateless 'gateway' server instances behind a load balancer, each holding a subset of the total connected users. The hard part is: when some backend service needs to notify a specific user, it has no way of knowing which of the many gateway instances currently holds that user's connection. I'd solve this with a pub/sub layer, like Redis Pub/Sub or Kafka, that all gateway instances subscribe to — the originating service just publishes 'notify user X' to that layer, and either every gateway instance checks if it holds a connection for user X and delivers it if so, or, at larger scale, I'd maintain a presence registry mapping user ID to the specific gateway instance holding their connection, so the message can be routed directly to just that one instance instead of broadcasting to all of them. I'd also need to handle connection state carefully — when a user disconnects and reconnects, possibly to a different gateway instance, that presence registry needs to be updated immediately."},"follow_up_hint":"Ask them how they'd handle a user with multiple simultaneous connections (e.g. open on both phone and laptop)."}
]

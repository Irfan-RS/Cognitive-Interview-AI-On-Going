[
  {
    "id": "oauth-0001",
    "question": "Explain the OAuth 2.0 Authorization Code flow, and why it's preferred over the Implicit flow for most web applications today.",
    "type": "conceptual",
    "difficulty": 4,
    "roles": [
      "backend",
      "security"
    ],
    "topics": [
      "security",
      "authentication"
    ],
    "tech_keywords": [
      "oauth2"
    ],
    "companies_common": [
      "Amazon",
      "Google",
      "Stripe"
    ],
    "source_style": "faang-common",
    "reference_solution": {
      "key_points": [
        "Authorization Code flow: the client redirects the user to the authorization server, which redirects back with a short-lived authorization code (not the token itself) after the user authenticates/consents",
        "The client's backend then exchanges that code for an access token (and refresh token) via a direct server-to-server call, authenticated with a client secret — the token itself never passes through the browser/URL",
        "Implicit flow returned the access token directly in the URL fragment after redirect, exposing it to browser history, referrer leaks, and any script on the page — considered insecure by modern standards",
        "Modern best practice: Authorization Code flow with PKCE (Proof Key for Code Exchange) even for public clients (SPAs, mobile apps) that can't securely hold a client secret, replacing the old Implicit flow entirely"
      ],
      "sample_answer": "In the Authorization Code flow, the client application redirects the user to the authorization server, which authenticates the user and, on consent, redirects back to the client with a short-lived authorization code — not the actual access token — as a URL parameter. The client's backend then makes a separate, direct server-to-server request to exchange that code for an access token, authenticating itself with a confidential client secret in that exchange, so the actual access token never passes through the user's browser or shows up in browser history or URL logs. The older Implicit flow instead returned the access token directly in the URL fragment after the redirect, which exposed it to real risks like browser history, referrer header leaks, and any malicious script that might be running on the page. Modern best practice today is using Authorization Code flow with PKCE even for public clients like single-page apps or mobile apps that can't safely store a client secret — PKCE adds a dynamically generated secret per authorization request instead, which is why the Implicit flow has been formally deprecated in favor of this."
    },
    "follow_up_hint": "Ask them to explain what PKCE specifically adds and how it prevents an intercepted authorization code from being exchanged by an attacker.",
    "concept": "OAuth 2.0 Flows",
    "sub_concept": "Authorization Code vs Implicit Flow Security Trade-offs",
    "expected_reasoning": "A strong candidate begins by outlining the end-to-end lifecycle of the Authorization Code flow, emphasizing how separating authentication from token delivery mitigates exposure risks. They analyze the structural vulnerability of the Implicit flow, specifically tracking how returning access tokens directly in URL fragments exposes them to browser history and XSS vectors. They then weigh the trust boundaries between front-end browsers and back-end servers, explaining why direct server-to-server token exchange is inherently safer. Finally, they incorporate modern security standards by explaining how PKCE extends the Authorization Code flow to public clients that cannot maintain a static client secret.",
    "common_mistakes": [
      "Confusing OAuth 2.0 authorization with OpenID Connect authentication",
      "Stating that the Implicit flow is faster or simpler without mentioning the severe security trade-offs",
      "Failing to explain where tokens are stored or exposed in transit for each flow",
      "Claiming that SPAs must use the Implicit flow because they cannot keep a client secret",
      "Omitting the role of the authorization code as an intermediary step that prevents token exposure in the browser"
    ],
    "progressive_hints": [
      "Consider where the access token actually travels in each flow and who has visibility into the URL parameters or browser environment.",
      "Think about what a malicious JavaScript script running on the page or an accidental leak in a browser referrer header can access during a redirect.",
      "Examine how the Authorization Code flow uses an intermediary artifact to avoid ever exposing long-lived access tokens directly to the browser front-end."
    ],
    "learning_objective": "Understand why the Authorization Code flow eliminates critical browser-based token exposure vectors present in the deprecated Implicit flow, and how modern standards apply it safely to all application types."
  },
  {
    "id": "oauth-0002",
    "question": "What is the difference between OAuth 2.0 and OpenID Connect (OIDC)?",
    "type": "conceptual",
    "difficulty": 3,
    "roles": [
      "backend",
      "security"
    ],
    "topics": [
      "security",
      "authentication"
    ],
    "tech_keywords": [
      "oauth2",
      "oidc"
    ],
    "companies_common": [
      "Amazon",
      "Google"
    ],
    "source_style": "faang-common",
    "reference_solution": {
      "key_points": [
        "OAuth 2.0 is an AUTHORIZATION framework — it lets a client obtain limited access to a resource on a user's behalf (e.g. 'this app can read your calendar'), but says nothing standardized about the user's identity",
        "OIDC is an AUTHENTICATION layer built on top of OAuth 2.0, adding a standardized ID token (a JWT) that securely conveys who the user actually is",
        "A common confusion/mistake: using a raw OAuth access token to determine 'who is this user' — access tokens are opaque-by-design and meant for authorization to an API, not for identity assertions",
        "OIDC's ID token specifically includes standardized claims (sub, email, name, etc.) meant for the client application to establish the user's identity, cryptographically signed by the identity provider"
      ],
      "sample_answer": "OAuth 2.0 is fundamentally an authorization framework — it's about letting a client application get limited, scoped access to some resource on a user's behalf, like 'this app is allowed to read your calendar', but it doesn't standardize anything about actually establishing who the user is. OpenID Connect is an authentication layer built directly on top of OAuth 2.0 that adds a standardized ID token, which is a signed JWT specifically meant to convey the user's identity, including standardized claims like their subject ID, email, and name. A common mistake is using a plain OAuth access token to try to determine who the user is — access tokens are meant to be opaque, scoped credentials for calling an API, not identity assertions, and using them that way isn't safe or standardized. OIDC's ID token exists specifically to solve the 'who is this user' problem properly, cryptographically signed by the identity provider so the client can verify it hasn't been tampered with."
    },
    "follow_up_hint": "Ask them what claims they'd expect to find inside a decoded OIDC ID token, and how the client verifies the token's signature.",
    "concept": "Authentication Vs Authorization",
    "sub_concept": "OAuth 2.0 and OpenID Connect Distinction",
    "expected_reasoning": "A strong candidate begins by separating the core security concerns of 'who are you' from 'what are you allowed to do.' They analyze OAuth 2.0 as an authorization framework designed to grant third-party applications limited access to resources without sharing credentials. Next, they recognize the limitation of OAuth in identifying the actual user, which motivates the introduction of OpenID Connect. Finally, they articulate how OIDC layers an identity protocol on top of OAuth by introducing a cryptographically signed ID token containing standard user claims.",
    "common_mistakes": [
      "Treating OAuth 2.0 as an authentication protocol rather than solely an authorization framework",
      "Believing OIDC is a completely separate alternative to OAuth 2.0 rather than an extension built on top of it",
      "Using raw OAuth access tokens to identify the user in the client application instead of using OIDC ID tokens",
      "Stating the conclusion with no justification, such as simply saying 'one is for login and one is for API access' without explaining tokens or scopes",
      "Confusing access tokens with ID tokens in terms of their intended audience and purpose"
    ],
    "progressive_hints": [
      "Consider the fundamental difference between granting an application permission to act on your behalf versus proving to the application who you are.",
      "Think about what information an OAuth access token actually carries and whether it was ever designed to tell an application who the user is.",
      "Look at how OpenID Connect extends the OAuth 2.0 authorization flow by adding a specific, standardized token format that conveys user identity claims."
    ],
    "learning_objective": "Understand that OAuth 2.0 handles authorization while OpenID Connect builds upon it to handle authentication through standardized identity tokens."
  },
  {
    "id": "oauth-0003",
    "question": "What is a refresh token, and why is it used instead of just issuing long-lived access tokens?",
    "type": "conceptual",
    "difficulty": 3,
    "roles": [
      "backend",
      "security"
    ],
    "topics": [
      "security",
      "authentication"
    ],
    "tech_keywords": [
      "oauth2",
      "jwt"
    ],
    "companies_common": [
      "Amazon",
      "Stripe"
    ],
    "source_style": "common-interview",
    "reference_solution": {
      "key_points": [
        "Access tokens are typically short-lived (minutes to a couple hours) to limit the damage window if one is stolen/leaked",
        "A refresh token is longer-lived and is used to obtain a new access token without requiring the user to log in again, kept more securely (often not exposed to client-side JS at all, stored server-side or in an httpOnly cookie)",
        "This balances security (short-lived access tokens limit blast radius of a leak) with usability (the user doesn't have to re-authenticate constantly)",
        "Refresh tokens can be revoked server-side, immediately invalidating a user's ongoing access, whereas a long-lived stateless access token (like an unrevoked JWT) generally can't be invalidated early without extra infrastructure (a revocation list)"
      ],
      "sample_answer": "Access tokens are deliberately short-lived, often just minutes to a couple hours, specifically to limit the damage window if one is ever stolen or leaked — an attacker only has a brief window to use it. A refresh token is longer-lived and is used solely to obtain a new access token when the current one expires, without forcing the user to log in again, and it's kept more securely than the access token — often never exposed to client-side JavaScript at all, stored instead server-side or in an httpOnly cookie. This gives a good balance: short-lived access tokens limit the blast radius if one leaks, while the refresh token preserves a good user experience by avoiding constant re-authentication. Another advantage is that a refresh token can be revoked server-side immediately if needed — like when a user logs out or an account is compromised — which effectively kills a user's ongoing access, whereas a long-lived, self-contained stateless access token like an unrevoked JWT typically can't be invalidated early without additional infrastructure like a token revocation list."
    },
    "follow_up_hint": "Ask them how they'd implement immediate token revocation for a stateless JWT access token, given JWTs are normally validated without a database lookup.",
    "concept": "Token Based Authentication",
    "sub_concept": "Access and Refresh Token Lifecycles",
    "expected_reasoning": "A strong candidate begins by analyzing the inherent tension in authentication design between security and user experience. They weigh the vulnerabilities of long-lived tokens against the friction of frequent re-authentication, then deduce how decoupling authorization credentials from session continuity resolves this conflict. They explain the architectural separation of storage and privileges between the short-lived access token and the securely stored refresh token, concluding with considerations for revocation and blast radius containment.",
    "common_mistakes": [
      "States that refresh tokens are just longer-lived access tokens with no architectural difference",
      "Claims access tokens are long-lived to save database lookup overhead without mentioning security trade-offs",
      "Fails to mention token revocation differences between stateless JWT access tokens and stateful refresh tokens",
      "Assumes both token types are stored identically in local storage or exposed to client-side JavaScript",
      "Confuses session cookies with OAuth refresh tokens without explaining their distinct lifecycles"
    ],
    "progressive_hints": [
      "Consider the competing priorities of keeping user sessions convenient while minimizing the risk if a malicious actor intercepts credentials.",
      "Think about what happens to an active session if a token is stolen, and how the duration of that token's validity impacts the damage window.",
      "What if you split authentication into two credentials: one used for every single API request, and another used exclusively to renew the first one?"
    ],
    "learning_objective": "Understand how separating short-lived access tokens from securely stored refresh tokens optimizes both API security blast radius and user session longevity."
  }
]

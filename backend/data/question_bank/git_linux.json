[
  {"id":"git-0001","question":"What is the difference between `git merge` and `git rebase`?","type":"conceptual","difficulty":1,"roles":["general_sde"],"topics":["git","version-control"],"tech_keywords":["git"],"companies_common":["Amazon","Microsoft","Infosys"],"source_style":"common-interview","reference_solution":{"key_points":["Merge creates a new merge commit joining two branch histories, preserving both branches' actual commit history","Rebase replays your branch's commits on top of another branch, producing a linear history with no merge commit","Rebase rewrites commit hashes, which is dangerous on shared/public branches since collaborators' histories diverge","Merge preserves true history (what actually happened, when); rebase produces a cleaner but rewritten history","Rule of thumb: rebase local/private branches before sharing, merge for integrating shared branches"],"sample_answer":"Merge combines two branches by creating a new merge commit that has both branches as parents, preserving exactly what happened and when. Rebase instead replays your branch's commits one by one on top of another branch's tip, producing a clean, linear history with no merge commit — but it rewrites commit hashes, which breaks history for anyone else who already has the old commits, so it should only be done on local/unshared branches."},"follow_up_hint":"Ask what 'golden rule of rebasing' means and why rebasing a shared branch is dangerous."},
  {"id":"git-0002","question":"How would you undo the last commit, keeping the changes staged, versus discarding them entirely?","type":"practical_implementation","difficulty":1,"roles":["general_sde"],"topics":["git"],"tech_keywords":["git"],"companies_common":["Amazon","Infosys"],"source_style":"common-interview","reference_solution":{"key_points":["`git reset --soft HEAD~1` undoes the commit but keeps changes staged","`git reset --mixed HEAD~1` (default) undoes the commit and unstages changes, keeping them in the working directory","`git reset --hard HEAD~1` discards the commit and all changes entirely — irreversible without reflog","Explains reflog as a safety net for recovering from an accidental hard reset","Distinguishes reset (rewrites local history) from revert (creates a new commit undoing changes, safe for shared history)"],"sample_answer":"`git reset --soft HEAD~1` removes the last commit but keeps its changes staged, ready to re-commit. `git reset --mixed HEAD~1` (the default) also unstages them, leaving changes in the working directory. `git reset --hard HEAD~1` discards the commit and its changes entirely — recoverable only via git reflog if at all. For a commit already pushed/shared, `git revert` is safer since it creates a new commit undoing the changes rather than rewriting history."},"follow_up_hint":"Ask what git reflog is and how it can recover a 'lost' commit after a hard reset."},
  {"id":"git-0003","question":"Explain what a merge conflict is and how you'd resolve one.","type":"practical_implementation","difficulty":1,"roles":["general_sde"],"topics":["git"],"tech_keywords":["git"],"companies_common":["Infosys","TCS","Amazon"],"source_style":"common-interview","reference_solution":{"key_points":["A merge conflict happens when the same lines were changed differently on both branches being merged, and Git can't auto-resolve it","Git marks the conflicting sections in the file with conflict markers (<<<<<<<, =======, >>>>>>>)","Resolution means manually editing the file to keep the correct combination of changes, removing the markers","After editing, stage the resolved file and complete the merge/rebase with a commit","Mentions using a merge tool or IDE integration to visualize conflicts more easily"],"sample_answer":"A merge conflict occurs when the same lines of a file were changed differently on both branches, and Git can't automatically decide which version is correct. Git marks the conflicting section directly in the file with <<<<<<<, =======, >>>>>>> markers showing both versions. Resolving it means manually editing the file to keep the right combination of changes and removing the markers, then staging the file and completing the merge or rebase with a commit."},"follow_up_hint":"Ask how they'd resolve a conflict during an interactive rebase versus a merge."},
  {"id":"git-0004","question":"What is the difference between `git fetch` and `git pull`?","type":"conceptual","difficulty":1,"roles":["general_sde"],"topics":["git"],"tech_keywords":["git"],"companies_common":["Infosys","Amazon"],"source_style":"common-interview","reference_solution":{"key_points":["git fetch downloads new commits/refs from the remote but doesn't change your local working branch","git pull is fetch followed by an automatic merge (or rebase, if configured) into your current branch","fetch lets you review incoming changes before integrating them; pull integrates immediately","Explains this matters when you want to see what changed upstream before merging it into your work"],"sample_answer":"git fetch downloads new commits and branch references from the remote repository but doesn't touch your current working branch at all. git pull does a fetch and then immediately merges (or rebases, depending on configuration) those changes into your current branch. Fetch is safer when you want to review what changed upstream before integrating it; pull is more convenient when you just want to stay in sync immediately."},"follow_up_hint":"Ask how to configure git pull to rebase instead of merge by default."},
  {"id":"git-0005","question":"Explain what git cherry-pick does and a scenario where you'd use it.","type":"practical_implementation","difficulty":2,"roles":["general_sde"],"topics":["git"],"tech_keywords":["git"],"companies_common":["Amazon","Microsoft"],"source_style":"common-interview","reference_solution":{"key_points":["cherry-pick applies a specific commit from one branch onto another, without merging the entire branch","Useful for backporting a bug fix to a release branch without pulling in unrelated feature commits","Creates a new commit with a new hash, even though the content is the same as the original","Can cause conflicts if the target branch has diverged in the same area"],"sample_answer":"git cherry-pick applies the changes from one specific commit onto your current branch, without merging the entire source branch. A common use case is backporting a critical bug fix from the main branch to an older release branch, where you want just that one fix, not the dozens of unrelated feature commits that came after it on main. It creates a brand-new commit with a new hash, even though the actual code change is identical to the original."},"follow_up_hint":"Ask what happens if the cherry-picked commit conflicts with the target branch."},
  {"id":"git-0006","question":"What is a Linux inode, and how does it relate to hard links versus symbolic links?","type":"conceptual","difficulty":2,"roles":["backend","devops"],"topics":["linux","operating-systems"],"tech_keywords":["linux"],"companies_common":["Amazon","Nvidia"],"source_style":"common-interview","reference_solution":{"key_points":["An inode stores a file's metadata (permissions, size, timestamps, data block pointers) but not its name","A filename is just a directory entry pointing to an inode","A hard link is another directory entry pointing to the SAME inode — indistinguishable from the 'original'","A symbolic link is a separate file containing a path string pointing to another file/path, with its own inode","Explains deleting the original file breaks a symlink (dangling link) but a hard link still works since the inode/data persists until all hard links are removed"],"sample_answer":"An inode holds a file's actual metadata — permissions, size, timestamps, and pointers to its data blocks — but not its name; a filename is just an entry in a directory mapping a name to an inode number. A hard link is another directory entry pointing to that exact same inode, so there's no real 'original' versus 'link' distinction — deleting one just removes that directory entry, and the data persists until all hard links to it are gone. A symbolic link is a different kind of file entirely, with its own inode, that just stores a path string pointing elsewhere — if that target is deleted or moved, the symlink becomes a dangling reference."},"follow_up_hint":"Ask why hard links can't span across different filesystems/partitions but symlinks can."},
  {"id":"git-0007","question":"Explain the difference between `chmod 755` and `chmod 644` and what the numbers mean.","type":"practical_implementation","difficulty":1,"roles":["backend","devops"],"topics":["linux"],"tech_keywords":["linux","chmod"],"companies_common":["Amazon","Infosys"],"source_style":"common-interview","reference_solution":{"key_points":["Each digit represents permissions for owner, group, and others respectively, as a sum of read(4)+write(2)+execute(1)","755 = owner rwx(7), group r-x(5), others r-x(5) — typical for an executable script/directory","644 = owner rw-(6), group r--(4), others r--(4) — typical for a regular non-executable file","Explains why directories need the execute bit (to be 'entered'/listed properly), distinct from files"],"sample_answer":"Each digit is a sum of read(4), write(2), and execute(1) permissions, in order for owner, group, and others. 755 means the owner has read+write+execute (7), while group and others have read+execute (5) — typical for an executable script or a directory, since directories need the execute bit to be traversed. 644 means the owner has read+write (6), and group/others have read-only (4) — typical for a regular data file that shouldn't be executed."},"follow_up_hint":"Ask what the execute permission actually means for a directory versus a regular file."},
  {"id":"git-0008","question":"How would you find which process is using a specific port on Linux, and how would you kill it?","type":"practical_implementation","difficulty":1,"roles":["backend","devops"],"topics":["linux"],"tech_keywords":["linux","networking"],"companies_common":["Amazon","Infosys"],"source_style":"common-interview","reference_solution":{"key_points":["`lsof -i :PORT` or `netstat -tulpn | grep PORT` lists the process using a given port","This reveals the process ID (PID) associated with that port","`kill PID` sends SIGTERM (graceful shutdown request); `kill -9 PID` sends SIGKILL (immediate, forceful termination)","Explains preferring SIGTERM first, since it lets the process clean up (close files, flush buffers) before exiting"],"sample_answer":"`lsof -i :PORT` (or `netstat -tulpn | grep PORT` on some systems) lists which process is bound to a given port, including its PID. From there, `kill PID` sends SIGTERM, a graceful shutdown request the process can catch and clean up after; `kill -9 PID` sends SIGKILL, which terminates it immediately with no chance to clean up. I'd generally try SIGTERM first and only escalate to SIGKILL if the process doesn't respond, since abrupt termination can leave files or resources in an inconsistent state."},"follow_up_hint":"Ask what happens to a process's open file descriptors and child processes when it's killed with SIGKILL."},
  {"id":"git-0009","question":"What is the difference between a process and a daemon on Linux?","type":"conceptual","difficulty":1,"roles":["backend","devops"],"topics":["linux","operating-systems"],"tech_keywords":["linux"],"companies_common":["Amazon"],"source_style":"common-interview","reference_solution":{"key_points":["A daemon is a background process, typically detached from any controlling terminal, running independently of user login sessions","Daemons often start at boot and run continuously providing a service (e.g. a web server, cron)","Explains a normal foreground process is tied to a terminal session and typically ends when that session closes","Naming convention: daemons often end in 'd' (sshd, httpd)"],"sample_answer":"A daemon is a background process detached from any controlling terminal, designed to run independently of any user's login session — it keeps running even after you log out. A normal foreground process is typically tied to the terminal session that started it and ends when that session closes. Daemons often start automatically at boot and provide an ongoing service, like sshd handling SSH connections or a cron daemon running scheduled jobs — by convention many daemon names end in 'd'."},"follow_up_hint":"Ask how a process detaches itself to become a daemon (double-fork, setsid)."},
  {"id":"git-0010","question":"Explain what a Git rebase --interactive is used for, and give an example of squashing commits.","type":"practical_implementation","difficulty":2,"roles":["general_sde"],"topics":["git"],"tech_keywords":["git"],"companies_common":["Amazon","Microsoft"],"source_style":"common-interview","reference_solution":{"key_points":["Interactive rebase lets you reorder, edit, squash, or drop commits before they're shared","Squashing combines multiple commits into one, useful for cleaning up a messy series of 'WIP'/fixup commits before merging","`git rebase -i HEAD~N` opens an editor listing the last N commits with actions (pick, squash, edit, drop) to choose per commit","Explains this rewrites history, so it should only be done before pushing/sharing the branch"],"sample_answer":"Interactive rebase lets you edit a series of commits before sharing them — reordering, editing messages, squashing multiple commits into one, or dropping unwanted ones entirely. Running `git rebase -i HEAD~3` opens an editor listing the last 3 commits, where you'd change 'pick' to 'squash' on the commits you want merged into the previous one, producing a single clean commit instead of a messy trail of 'fix typo' or 'WIP' commits. Since this rewrites commit history, it should only be done on commits that haven't been pushed/shared yet."},"follow_up_hint":"Ask what happens if you try to interactively rebase commits that have already been pushed and others have pulled."}
]

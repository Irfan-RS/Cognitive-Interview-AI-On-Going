[
  {"id":"search-0001","question":"Why would you use a dedicated search engine like Elasticsearch instead of just using SQL LIKE queries against a relational database?","type":"trade_off_questions","difficulty":3,"roles":["backend","system_design"],"topics":["search"],"tech_keywords":["elasticsearch"],"companies_common":["Amazon","Uber"],"source_style":"faang-common","reference_solution":{"key_points":["SQL LIKE queries (especially with leading wildcards, e.g. LIKE '%term%') generally can't use a standard B-tree index efficiently, forcing a full table/column scan that gets slow at scale","Elasticsearch uses an inverted index (mapping each word to the documents/rows containing it), purpose-built for fast full-text search across large volumes of text","Provides relevance ranking out of the box (scoring how well a document matches a query, not just a binary match/no-match), plus fuzzy matching, typo tolerance, and language-aware tokenization/stemming","Trade-off: adds an additional system to operate and keep in sync with the source-of-truth database (usually via a change-data-capture or dual-write pipeline), introducing eventual consistency between the two"],"sample_answer":"A SQL LIKE query, especially one with a leading wildcard like '%searchterm%', generally can't use a standard database index efficiently at all, forcing a full scan across the column or table, which gets prohibitively slow as data grows. Elasticsearch is purpose-built for this: it maintains an inverted index, mapping each individual word to all the documents containing it, which makes full-text search across large volumes of text fast regardless of scale. It also provides relevance ranking out of the box — scoring how well each result actually matches the query rather than just a binary yes/no match — along with features like fuzzy matching for typo tolerance and language-aware tokenization and stemming, which a plain relational LIKE query has no concept of at all. The trade-off is that adding Elasticsearch means operating and maintaining an entirely separate system that needs to be kept in sync with your actual source-of-truth database, typically through a change-data-capture pipeline or dual writes, which introduces some eventual consistency between the primary database and what's searchable in Elasticsearch at any given moment."},"follow_up_hint":"Ask them how they'd keep Elasticsearch's index in sync with the primary database as records are created, updated, and deleted."}
]

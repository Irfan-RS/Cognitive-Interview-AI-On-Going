[
  {"id":"nosql-0001","question":"When would you choose a NoSQL database over a relational database?","type":"trade_off_questions","difficulty":2,"roles":["backend","system_design"],"topics":["dbms","system-design"],"tech_keywords":["nosql","mongodb"],"companies_common":["Amazon","Uber"],"source_style":"common-interview","reference_solution":{"key_points":["Relational DBs excel at structured data with well-defined relationships, strong consistency (ACID transactions), and complex queries/joins","NoSQL fits use cases needing flexible/evolving schemas, horizontal scalability across many nodes, or specific access patterns (key-value lookups, document storage, wide-column, graph) that map awkwardly to tables","Choose NoSQL when: schema changes frequently, extremely high write throughput/horizontal scale is required, or data is naturally hierarchical/document-shaped (e.g. user profiles with nested, varying structure)","Choose relational when: data integrity/relationships matter a lot, complex multi-table queries are common, and the schema is relatively stable"],"sample_answer":"I'd lean toward a relational database when data has clear structured relationships, when strong consistency via ACID transactions matters, and when the application needs complex queries joining multiple tables — like most financial or inventory systems. I'd lean toward NoSQL when the schema is likely to evolve frequently and rigidly defining it upfront is painful, when the access pattern is naturally document- or key-value-shaped, like storing user profiles with deeply nested and varying fields, or when I need to scale writes horizontally across many nodes more easily than a traditional single-primary relational setup typically allows. It's not that NoSQL databases can't do relationships or that relational databases can't scale — it's about which trade-offs and access patterns best match the actual workload."},"follow_up_hint":"Ask them to design the schema for a product catalog with wildly varying attributes per category, and justify document vs relational."},
  {"id":"nosql-0002","question":"Explain the difference between a document database (like MongoDB) and a wide-column database (like Cassandra).","type":"conceptual","difficulty":3,"roles":["backend","system_design"],"topics":["dbms"],"tech_keywords":["mongodb","cassandra"],"companies_common":["Amazon","Uber"],"source_style":"faang-common","reference_solution":{"key_points":["Document databases store self-contained JSON/BSON-like documents with flexible, often nested structure, typically queried by any field within the document","Wide-column stores organize data into rows identified by a key, but with flexible sets of columns per row, grouped into column families — optimized heavily for very fast writes and scans over specific partition/row keys","Document DBs suit general-purpose flexible-schema applications; wide-column stores suit massive-scale, high-write-throughput, time-series-like or partition-key-driven access patterns","Cassandra specifically is designed for high availability and linear write scalability across many nodes (masterless, eventually consistent by default)"],"sample_answer":"A document database like MongoDB stores self-contained documents, often JSON-like with nested structure, and generally lets you query flexibly on any field within a document. A wide-column store like Cassandra organizes data into rows identified by a key, but each row can have a different flexible set of columns grouped into column families, and it's architected specifically for extremely fast writes and scans that are driven by a partition/row key. I'd reach for a document database for general-purpose applications needing schema flexibility and reasonably rich querying, and for a wide-column store like Cassandra specifically when I need massive write throughput and horizontal scalability across many nodes with high availability, like time-series data or event logging at very large scale, where Cassandra's masterless, eventually-consistent-by-default architecture is a good fit."},"follow_up_hint":"Ask them why Cassandra's data modeling approach requires designing your schema around your query patterns upfront, unlike a relational database."},
  {"id":"nosql-0003","question":"What is denormalization, and why is it common in NoSQL data modeling?","type":"trade_off_questions","difficulty":3,"roles":["backend"],"topics":["dbms","nosql"],"tech_keywords":["denormalization"],"companies_common":["Amazon","Uber"],"source_style":"faang-common","reference_solution":{"key_points":["Denormalization means duplicating data across multiple places instead of normalizing it into separate, joined tables — trading storage/write complexity for read performance","Many NoSQL databases don't support efficient joins (or any joins at all), so retrieving related data efficiently often requires embedding/duplicating it directly in the document/row you'll query","Trade-off: writes become more complex (must update duplicated copies consistently) and storage increases, but reads become much faster/simpler since no cross-collection joins are needed at query time","Design principle in NoSQL modeling: model your schema around your query patterns, not around eliminating redundancy the way relational normalization does"],"sample_answer":"Denormalization means intentionally duplicating data across multiple places rather than storing it once and joining it at query time. It's common in NoSQL modeling because many NoSQL databases either don't support efficient joins or don't support them at all, so if a query needs related data — like an order document needing the customer's name and address — it's often much faster to embed a copy of that data directly in the order document rather than doing a separate lookup. The trade-off is that writes become more complex, since updating a customer's address now potentially means updating it in every order document that embedded a copy, and storage usage increases due to the duplication — but reads become simpler and much faster since there's no join needed at query time. This reflects a broader NoSQL modeling philosophy: design your schema around how the data will actually be queried, rather than around eliminating redundancy the way relational normalization does."},"follow_up_hint":"Ask them how they'd handle keeping duplicated data eventually consistent when the source of truth changes."}
]

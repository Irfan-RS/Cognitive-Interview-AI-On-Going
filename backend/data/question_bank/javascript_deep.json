[
  {"id":"jsd-0001","question":"Explain JavaScript closures with a practical example.","type":"conceptual","difficulty":2,"roles":["frontend","backend"],"topics":["javascript"],"tech_keywords":["javascript","closures"],"companies_common":["Amazon","Meta","Google"],"source_style":"common-interview","reference_solution":{"key_points":["A closure is a function that retains access to variables from its enclosing lexical scope, even after that outer function has finished executing","This happens because JavaScript functions capture references to their surrounding scope at creation time, not just a snapshot of values","Practical example: a counter factory function that returns an increment function, where each returned function keeps its own private, persistent count variable inaccessible from outside","Commonly used for data privacy/encapsulation (private variables), and historically for the module pattern before ES modules existed"],"sample_answer":"A closure is a function that retains access to variables from the scope it was defined in, even after that outer, enclosing function has already finished running and returned. This works because JavaScript functions capture a live reference to their surrounding lexical scope at the time they're created, not just a one-time snapshot of the values. A classic practical example is a counter factory: `function makeCounter() { let count = 0; return () => ++count; }` — the returned function keeps access to that specific `count` variable, incrementing and remembering it across calls, even though `makeCounter` itself has already returned. This is commonly used for data privacy, keeping a variable like count truly private and inaccessible from outside code except through the specifically returned function, and it was the basis of the module pattern for encapsulation before ES modules existed natively."},"follow_up_hint":"Ask them the classic 'var in a loop' closure gotcha and how let versus var changes the behavior."},
  {"id":"jsd-0002","question":"What is the difference between `==` and `===` in JavaScript, and why is `===` generally recommended?","type":"conceptual","difficulty":1,"roles":["frontend","backend"],"topics":["javascript"],"tech_keywords":["javascript"],"companies_common":["Amazon","Infosys"],"source_style":"common-interview","reference_solution":{"key_points":["== performs type coercion before comparing — it converts operands to a common type if they differ, which can produce surprising results (e.g. '' == 0 is true, null == undefined is true)","=== compares both value and type directly, with no coercion — if types differ, it's simply false, no exceptions","=== is generally recommended because coercion rules are complex and non-obvious, making == a common source of subtle bugs","Gives at least one concrete surprising == example to demonstrate the risk"],"sample_answer":"The == operator performs type coercion before comparing two values — if the operands are different types, JavaScript converts them to a common type first, which can lead to surprising results, like '' == 0 evaluating to true, or null == undefined also being true. The === operator compares both the value and the type directly with no coercion at all — if the types differ, it's simply false, with no conversion happening behind the scenes. === is generally recommended because the coercion rules behind == are genuinely complex and non-obvious even to experienced developers, making == a common source of subtle, hard-to-spot bugs, whereas === behaves predictably and explicitly."},"follow_up_hint":"Ask them what NaN == NaN evaluates to, and why, as a further example of coercion's surprising edge cases."},
  {"id":"jsd-0003","question":"Explain JavaScript's prototype chain and how it enables inheritance.","type":"conceptual","difficulty":3,"roles":["frontend","backend"],"topics":["javascript"],"tech_keywords":["javascript","prototypes"],"companies_common":["Amazon","Meta"],"source_style":"faang-common","reference_solution":{"key_points":["Every JS object has an internal link to another object, its prototype; when a property/method isn't found directly on an object, JS looks up the chain to the prototype, then the prototype's prototype, and so on","This chain ultimately ends at Object.prototype, whose own prototype is null, terminating the lookup","Object.create(), constructor functions with .prototype, or the 'class' syntax (syntactic sugar over prototypes) are different ways to set up this prototype linkage for inheritance","class syntax in modern JS doesn't change this underlying mechanism — it's still prototype-based inheritance, just with more familiar, class-like syntax on top"],"sample_answer":"Every JavaScript object has an internal link to another object called its prototype. When you access a property or method on an object and it's not found directly on that object itself, JavaScript automatically looks up the chain to its prototype, then that prototype's own prototype, and so on, until it either finds the property or reaches the end of the chain, which terminates at Object.prototype, whose own prototype is null. This chain is what implements inheritance in JavaScript: you can set an object's prototype via Object.create(), through a constructor function's .prototype property, or using the more familiar 'class' syntax introduced in ES6 — but it's important to know that 'class' is really just syntactic sugar over this same underlying prototype-based mechanism, not a fundamentally different inheritance model like in classical OOP languages."},"follow_up_hint":"Ask them to explain what `hasOwnProperty` checks for, in contrast to a property found via the prototype chain."},
  {"id":"jsd-0004","question":"Explain the difference between `null` and `undefined` in JavaScript.","type":"conceptual","difficulty":1,"roles":["frontend"],"topics":["javascript"],"tech_keywords":["javascript"],"companies_common":["Amazon","Infosys"],"source_style":"common-interview","reference_solution":{"key_points":["undefined: a variable has been declared but not yet assigned a value, or a function parameter wasn't provided, or accessing a non-existent object property — JS's own default for 'no value yet'","null: an explicitly assigned value representing 'intentionally no value' — set deliberately by the programmer to indicate absence","typeof null is (confusingly, due to a long-standing JS bug) 'object', while typeof undefined is 'undefined'","== considers null and undefined equal to each other, but === does not, since they're different types"],"sample_answer":"undefined typically represents the absence of a value that JavaScript itself assigns by default — a declared variable that hasn't been given a value yet, a function parameter that wasn't passed an argument, or accessing a property that doesn't exist on an object. null, on the other hand, is a value that's explicitly assigned by the programmer to deliberately represent 'intentionally no value' or 'nothing here on purpose'. One quirky detail is that typeof null returns 'object', which is actually a long-standing bug baked into the language from its earliest days that's kept for backward compatibility, while typeof undefined correctly returns 'undefined'. Using ==, null and undefined are considered equal to each other, but using ===, they're not, since they're different types."},"follow_up_hint":"Ask them why checking `if (value == null)` is sometimes used deliberately to catch both null and undefined in one check."}
]

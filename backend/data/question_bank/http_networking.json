[
  {"id":"http-0001","question":"Explain what happens step by step when you type a URL into a browser and press enter.","type":"conceptual","difficulty":2,"roles":["frontend","backend","general_sde"],"topics":["networking","http"],"tech_keywords":["http","dns","tcp"],"companies_common":["Amazon","Google","Microsoft"],"source_style":"faang-common","reference_solution":{"key_points":["DNS resolution: browser resolves the domain name to an IP address (checking cache, then recursive resolver, then authoritative servers)","TCP connection: browser establishes a TCP connection to that IP via a three-way handshake (SYN, SYN-ACK, ACK), plus a TLS handshake if HTTPS","HTTP request/response: browser sends an HTTP request, server processes it and returns a response (HTML, status code, headers)","Browser parses the HTML, discovers additional resources (CSS, JS, images), fetches them (often in parallel), then renders the page (DOM/CSSOM construction, layout, paint)"],"sample_answer":"First, the browser needs to resolve the domain name to an IP address via DNS — checking its own cache, then querying a recursive resolver, which in turn queries root, TLD, and authoritative name servers if needed. Once it has the IP, the browser opens a TCP connection to the server via a three-way handshake, and if it's HTTPS, performs a TLS handshake on top of that to establish an encrypted channel. Then the browser sends an HTTP request, the server processes it and returns a response with a status code, headers, and typically an HTML body. The browser parses that HTML, discovers additional resources it references like CSS, JavaScript, and images, fetches those — often in parallel — and finally renders the page by building the DOM and CSSOM, computing layout, and painting pixels to the screen."},"follow_up_hint":"Ask them to explain the TLS handshake in more detail, or what HTTP/2 or HTTP/3 change about this process."},
  {"id":"http-0002","question":"What is the difference between TCP and UDP, and when would you choose one over the other?","type":"trade_off_questions","difficulty":2,"roles":["backend","general_sde"],"topics":["networking"],"tech_keywords":["tcp","udp"],"companies_common":["Amazon","Google","Microsoft"],"source_style":"common-interview","reference_solution":{"key_points":["TCP: connection-oriented, guarantees reliable, ordered delivery via acknowledgments and retransmission; adds overhead (handshake, flow/congestion control)","UDP: connectionless, no delivery/ordering guarantees, no retransmission — but much lower latency and overhead","Choose TCP when correctness/completeness matters more than latency: web pages, file transfer, most APIs","Choose UDP when low latency matters more than occasional lost packets: live video/audio streaming, online gaming, DNS lookups"],"sample_answer":"TCP is connection-oriented and guarantees reliable, in-order delivery — it uses acknowledgments and retransmits lost packets, and includes flow and congestion control, but that reliability comes with more overhead and latency, including an initial handshake. UDP is connectionless and gives no guarantees about delivery or ordering — packets can be lost or arrive out of order and nothing retransmits them — but it has much lower overhead and latency since there's no handshake or acknowledgment machinery. I'd choose TCP for anything where correctness and completeness matter more than raw speed, like loading a web page, transferring a file, or most typical API calls. I'd choose UDP where low latency matters more than occasionally losing some data, like live video or audio streaming, real-time multiplayer gaming, or DNS lookups, where a dropped packet just gets retried at the application level if needed."},"follow_up_hint":"Ask why DNS primarily uses UDP but falls back to TCP for larger responses."},
  {"id":"http-0003","question":"What is CORS, and why does a browser block a cross-origin request by default?","type":"conceptual","difficulty":2,"roles":["frontend","backend"],"topics":["networking","security"],"tech_keywords":["cors"],"companies_common":["Amazon","Meta"],"source_style":"common-interview","reference_solution":{"key_points":["Same-origin policy: browsers restrict scripts on one origin (scheme+domain+port) from freely reading responses from a different origin, as a security measure against malicious sites making requests using a victim's credentials","CORS is a mechanism for a server to explicitly opt into allowing cross-origin requests from specified origins, via response headers like Access-Control-Allow-Origin","A 'preflight' OPTIONS request is sent by the browser first for non-simple requests, checking with the server whether the actual request is permitted before sending it","Clarifies CORS is enforced by the browser, not a server-side security mechanism itself — a non-browser client (like curl or a server-to-server call) isn't restricted by CORS at all"],"sample_answer":"Browsers enforce a same-origin policy: a script running on one origin — a specific scheme, domain, and port combination — is restricted from freely reading responses from a different origin, to prevent a malicious website from silently making authenticated requests to another site using the victim's logged-in session and reading the response. CORS is the mechanism that lets a server explicitly opt into allowing specific cross-origin requests, by returning headers like Access-Control-Allow-Origin naming which origins are permitted. For requests that aren't 'simple' — like ones using custom headers or certain HTTP methods — the browser first sends an OPTIONS 'preflight' request to check with the server whether the actual request would be allowed, before sending it. It's worth noting CORS is purely a browser-enforced restriction — a server-to-server call or a tool like curl isn't subject to CORS at all, since there's no browser enforcing same-origin policy in that context."},"follow_up_hint":"Ask what the security risk would actually be if CORS didn't exist and every cross-origin request were allowed by default."},
  {"id":"http-0004","question":"Explain the difference between HTTP status codes 401 and 403.","type":"conceptual","difficulty":1,"roles":["backend"],"topics":["http","security"],"tech_keywords":["http"],"companies_common":["Amazon","Infosys"],"source_style":"common-interview","reference_solution":{"key_points":["401 Unauthorized: the request lacks valid authentication credentials — the server doesn't know who you are (despite the confusing name, it's really about authentication)","403 Forbidden: the server knows who you are (authentication succeeded or wasn't required) but you don't have permission to access the resource (authorization failure)","A typical flow: no/invalid token → 401; valid token but insufficient role/permissions → 403","Mentions the historically confusing naming (401 is about authentication, not literally 'unauthorized' in the everyday sense)"],"sample_answer":"401 Unauthorized means the request doesn't include valid authentication credentials at all — despite the name, it's really about authentication, meaning the server doesn't know who's making the request. 403 Forbidden means the server does know who you are — you're authenticated, or authentication wasn't required — but you don't have permission to access that specific resource, which is an authorization failure. So a typical flow is: no token or an invalid/expired token returns 401, prompting the client to log in or refresh credentials; a valid token but insufficient role or permissions for that particular action returns 403, which login/re-authentication won't fix."},"follow_up_hint":"Ask what the correct response code should be for a resource that doesn't exist at all versus one the user isn't allowed to know exists."},
  {"id":"http-0005","question":"What is HTTP/2 multiplexing, and what problem does it solve compared to HTTP/1.1?","type":"conceptual","difficulty":3,"roles":["backend","frontend"],"topics":["http","networking"],"tech_keywords":["http2"],"companies_common":["Google","Amazon"],"source_style":"faang-common","reference_solution":{"key_points":["HTTP/1.1 typically allows only one outstanding request per TCP connection at a time (or a small number of parallel connections, since browsers cap concurrent connections per host), causing head-of-line blocking at the HTTP level","HTTP/2 multiplexes many requests and responses over a single TCP connection concurrently, interleaving frames from different streams, removing the need for multiple connections to the same host","This reduces connection overhead (fewer TCP/TLS handshakes) and avoids the need for old workarounds like domain sharding or spriting used to bypass HTTP/1.1's connection limits","Notes HTTP/2 still has TCP-level head-of-line blocking if a packet is lost, which HTTP/3 (built on QUIC/UDP) addresses further"],"sample_answer":"In HTTP/1.1, generally only one request can be outstanding per TCP connection at a time, so browsers work around this by opening several parallel connections to the same host — but that's capped, and led to workarounds like domain sharding or combining files together just to get more parallelism. HTTP/2 introduces multiplexing: multiple requests and responses can be in flight concurrently over a single TCP connection, with data from different logical 'streams' interleaved as frames, so you no longer need multiple connections to the same host, and the old workarounds become counterproductive. This reduces overhead from repeated TCP and TLS handshakes and generally improves page load performance. One remaining limitation is that HTTP/2 still sits on top of TCP, so a single lost packet can still block all the multiplexed streams at the TCP level — which is part of why HTTP/3, built on QUIC over UDP, was developed to solve that remaining head-of-line blocking too."},"follow_up_hint":"Ask what head-of-line blocking specifically means and how HTTP/3's use of QUIC over UDP addresses it."}
]

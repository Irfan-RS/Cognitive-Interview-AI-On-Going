[
  {"id":"perf-0001","question":"What are Core Web Vitals, and what does each metric measure?","type":"conceptual","difficulty":2,"roles":["frontend"],"topics":["performance","frontend"],"tech_keywords":["web-vitals"],"companies_common":["Google","Meta","Amazon"],"source_style":"common-interview","reference_solution":{"key_points":["Largest Contentful Paint (LCP): time until the largest visible content element renders — measures perceived loading speed","Interaction to Next Paint (INP, replacing First Input Delay): measures responsiveness — how quickly the page responds to user interactions throughout its lifetime, not just the first one","Cumulative Layout Shift (CLS): measures visual stability — how much unexpected layout shifting occurs as the page loads (e.g. content jumping around as images/ads load in)","Explains these are used by Google as real user-experience signals, including as a ranking factor for search"],"sample_answer":"Largest Contentful Paint, or LCP, measures how long it takes for the largest visible content element on the page — often a hero image or main heading — to fully render, giving a good proxy for perceived loading speed. Interaction to Next Paint, INP, which replaced First Input Delay, measures overall responsiveness by tracking how quickly the page responds to user interactions across its entire lifetime, not just the very first click. Cumulative Layout Shift, CLS, measures visual stability — how much content unexpectedly shifts around during loading, like when an ad or image loads late and pushes text down right as a user was about to tap something. Google uses these as real user-experience signals, and they factor into search ranking, which is part of why they've become a standard set of metrics teams actively optimize for."},"follow_up_hint":"Ask them what specific technique reduces CLS caused by images loading without a reserved size (e.g. explicit width/height attributes)."},
  {"id":"perf-0002","question":"What techniques would you use to improve the initial load time of a large JavaScript web application?","type":"scenario_based","difficulty":3,"roles":["frontend"],"topics":["performance"],"tech_keywords":["code-splitting","lazy-loading"],"companies_common":["Meta","Amazon","Google"],"source_style":"faang-common","reference_solution":{"key_points":["Code splitting: break the JS bundle into smaller chunks loaded on demand (e.g. per route), so the initial load only downloads what's needed for the first screen, not the entire application","Lazy loading: defer loading of below-the-fold images, non-critical components, or third-party scripts until they're actually needed/visible","Tree-shaking and removing unused dependencies to reduce overall bundle size; using lighter alternative libraries where a heavy dependency isn't fully needed","Server-side rendering or static generation for the initial page to show meaningful content faster, before the full JS bundle even finishes loading and hydrating"],"sample_answer":"I'd start with code splitting — breaking the JavaScript bundle into smaller chunks, typically per route or per major feature, so the initial page load only downloads the code actually needed for that first screen, rather than the entire application upfront. I'd add lazy loading for below-the-fold images, non-critical UI components, and third-party scripts, so they only load once they're actually needed or about to become visible, rather than blocking the initial render. I'd also audit the bundle for unused dependencies and rely on tree-shaking to strip out dead code, and look for lighter-weight alternatives to any unusually heavy library that's only using a small fraction of its features. For content that needs to appear as fast as possible, I'd consider server-side rendering or static site generation for the initial page, so meaningful content shows up before the full JavaScript bundle has even finished downloading and hydrating on the client."},"follow_up_hint":"Ask them how they'd actually measure whether these changes made a real difference (e.g. Lighthouse, real user monitoring)."},
  {"id":"perf-0003","question":"Explain what browser rendering critical path means and how render-blocking resources affect it.","type":"conceptual","difficulty":3,"roles":["frontend"],"topics":["performance"],"tech_keywords":["critical-rendering-path"],"companies_common":["Google","Meta"],"source_style":"faang-common","reference_solution":{"key_points":["Critical rendering path: the sequence of steps the browser must complete before it can paint pixels to the screen — parsing HTML into the DOM, parsing CSS into the CSSOM, combining them into a render tree, layout, then paint","CSS is render-blocking by default: the browser won't paint anything until it has the CSSOM, since styles could affect what's visible, so a slow-loading stylesheet delays first paint","A <script> tag without async/defer is also render-blocking and parser-blocking: the HTML parser stops entirely to fetch and execute it before continuing, unless deferred","Techniques to shorten the critical path: minimize/inline critical CSS, defer/async non-critical JS, reduce the number of render-blocking resources and their size"],"sample_answer":"The critical rendering path is the sequence of steps a browser must go through before it can actually paint pixels to the screen: parsing the HTML into the DOM, parsing CSS into the CSSOM, combining both into a render tree, computing layout, and finally painting. CSS is render-blocking by default — the browser deliberately won't paint anything until it has the full CSSOM, since a late-arriving stylesheet could change what's visually correct to show, so a slow stylesheet directly delays the first paint. A regular <script> tag without async or defer is both parser-blocking and can be render-blocking too: the HTML parser stops entirely to fetch and execute that script before continuing to parse the rest of the page. To shorten the critical path, I'd inline the small amount of CSS actually needed for above-the-fold content directly in the HTML head, defer or async-load everything else, defer non-critical JavaScript so it doesn't block parsing, and generally minimize both the number and size of render-blocking resources needed before the first meaningful paint."},"follow_up_hint":"Ask them the specific difference in behavior between the `async` and `defer` script attributes."}
]

[
  {"id":"sdc-0001","question":"Design a URL shortening service like bit.ly. Walk through your high-level approach.","type":"system_design","difficulty":4,"roles":["backend","system_design"],"topics":["system-design"],"tech_keywords":["url-shortener"],"companies_common":["Amazon","Google","Uber"],"source_style":"faang-common","reference_solution":{"key_points":["Core requirement: given a long URL, generate a short unique code; given the short code, redirect to the original URL (a 301/302 redirect)","Generating the short code: either a base62-encoded auto-incrementing ID (guarantees uniqueness, simple), or a hash of the URL with collision handling — auto-increment ID approach is usually simpler and preferred","Storage: a simple key-value mapping (short code -> long URL) fits well in a key-value store or a simple relational table indexed on the short code, given the access pattern is a fast point lookup","At scale: heavy read traffic (redirects vastly outnumber creates) means a caching layer (Redis) in front of the database is valuable, and the redirect service itself should be a thin, horizontally-scalable stateless layer","Should ask clarifying questions: expected scale (reads/writes per second), whether custom aliases are needed, whether links expire, whether click analytics are required"],"sample_answer":"First I'd clarify scale and requirements: how many URLs created per day, read-to-write ratio, whether custom short codes and expiration are needed, and whether click analytics matter. For the core design, I'd generate short codes using a base62-encoded auto-incrementing ID from a counter service, which guarantees uniqueness without needing collision detection, unlike hashing the URL, which would need collision handling if two URLs hash to the same short code. I'd store the mapping from short code to original URL in a key-value store or a simple indexed table, since the primary access pattern is a fast point lookup by short code. Given that redirects almost always vastly outnumber URL creations, I'd put a caching layer like Redis in front of the database for the hottest short codes, and make the redirect-serving layer itself stateless and horizontally scalable behind a load balancer, since it's a simple read-heavy, latency-sensitive path. I'd also consider whether the counter service generating IDs needs to be distributed to avoid becoming a bottleneck or single point of failure at very high creation rates."},"follow_up_hint":"Ask them how they'd generate unique IDs at scale without the ID-generation service becoming a single point of failure or bottleneck."},
  {"id":"sdc-0002","question":"Design a rate limiter that can be used as a shared library across multiple microservices. What would your API look like and how would you implement it?","type":"system_design","difficulty":4,"roles":["backend","system_design"],"topics":["system-design"],"tech_keywords":["rate-limiting"],"companies_common":["Amazon","Stripe"],"source_style":"faang-common","reference_solution":{"key_points":["API design: a simple `allow(key) -> bool` interface, where 'key' identifies the entity being limited (user ID, IP, API key)","Algorithm choice: token bucket is often preferred for its ability to allow controlled bursts while enforcing a steady average rate","Storage backend: an in-memory implementation works for single-instance use, but a shared Redis backend is needed for consistent limits across multiple service instances","Design considerations: configurable limits per key/tier (different rate limits for free vs paid users), graceful behavior when the storage backend itself is unavailable (fail open vs fail closed), and returning informative response headers (remaining quota, reset time) to callers"],"sample_answer":"I'd expose a simple interface like `allow(key: string) -> bool`, where the key identifies whatever's being rate-limited — a user ID, an IP address, or an API key — keeping the rate limiting logic decoupled from any specific service's business logic. For the algorithm, I'd implement token bucket, since it naturally allows a controlled burst of requests up to the bucket's capacity while still enforcing a steady average rate over time, which tends to match real usage patterns better than a strict fixed window. For the storage backend, an in-memory implementation would work if a service only ever ran as a single instance, but since we're talking about a library shared across multiple microservice instances behind a load balancer, I'd back it with a shared Redis instance using atomic operations, so all instances checking the same key see a consistent, correct count. I'd also make limits configurable per key/tier, so a paid customer could have a higher limit than a free one, decide explicit behavior for when Redis itself is temporarily unavailable — likely failing open for a low-risk API but failing closed for something sensitive — and return standard response headers indicating remaining quota and reset time so callers can behave well and back off proactively."},"follow_up_hint":"Ask them to compare the token bucket approach to a sliding window log in terms of memory usage at very high request volumes."},
  {"id":"sdc-0003","question":"Design a simplified version of a news feed system, like a Twitter/X timeline. How would you handle generating each user's feed efficiently?","type":"system_design","difficulty":5,"roles":["backend","system_design"],"topics":["system-design"],"tech_keywords":["feed-generation","fan-out"],"companies_common":["Meta","Twitter","Uber"],"source_style":"faang-common","reference_solution":{"key_points":["Fan-out on write (push model): when a user posts, immediately push that post into the precomputed feed/inbox of all their followers — makes reading a feed very fast (just fetch the precomputed list), but expensive for users with millions of followers (a single post triggers millions of writes)","Fan-out on read (pull model): a user's feed is computed on demand at read time by fetching recent posts from everyone they follow and merging/sorting them — cheaper on write, but slower/more expensive on each read, especially for users following many people","Hybrid approach: push model for most users, but pull model (or a special-cased path) for celebrities/very-high-follower accounts to avoid the fan-out explosion on write — most large-scale systems use this hybrid","Should also discuss ranking (chronological vs algorithmic relevance-based ordering) and caching precomputed feeds"],"sample_answer":"There are two core strategies. Fan-out on write pushes a new post immediately into every follower's precomputed feed inbox as soon as it's created, so reading a feed is very fast — you're just fetching an already-built list. The problem is a user with millions of followers triggers millions of writes for a single post, which doesn't scale well. Fan-out on read instead computes a user's feed at request time, by fetching recent posts from everyone they follow and merging and sorting them on the fly, which is cheap on the write side but can be slow on reads, especially for a user following a very large number of accounts. In practice, most large-scale systems use a hybrid: fan-out on write for the vast majority of users, since most people have a manageable follower count, but switch to a pull-based or specially-handled path for celebrity accounts with huge follower counts, avoiding the fan-out explosion for just those cases. I'd also cache each user's precomputed feed and consider whether the feed should just be reverse-chronological or ranked by an algorithmic relevance score, which adds significant additional complexity around ranking model serving."},"follow_up_hint":"Ask them how they'd handle a user unfollowing someone whose posts are already sitting in their precomputed feed."}
]

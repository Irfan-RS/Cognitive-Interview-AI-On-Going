[
  {"id":"idx-0001","question":"How does a database index speed up query performance, and what's the trade-off of adding one?","type":"trade_off_questions","difficulty":2,"roles":["backend"],"topics":["dbms","sql"],"tech_keywords":["database-index"],"companies_common":["Amazon","Infosys"],"source_style":"common-interview","reference_solution":{"key_points":["Without an index, the database must scan every row (full table scan) to find matching rows for a query","An index (typically a B-tree) maintains a sorted structure mapping column values to row locations, letting the database find matches in O(log n) instead of O(n)","Trade-off: every index adds overhead to writes (INSERT/UPDATE/DELETE must also update the index), and consumes additional storage","Indexes should be added based on actual query patterns (columns frequently used in WHERE/JOIN/ORDER BY), not indiscriminately on every column"],"sample_answer":"Without an index, the database has to scan every row in the table to find the ones matching a query's condition — a full table scan, which is O(n). An index, usually implemented as a B-tree, maintains a sorted structure mapping column values to their row locations, so the database can find matching rows in roughly O(log n) time instead. The trade-off is that every index has to be updated whenever a row is inserted, updated, or deleted, so more indexes mean slower writes, plus each index consumes additional disk space. So indexes should be added deliberately based on actual query patterns — columns frequently used in WHERE clauses, JOIN conditions, or ORDER BY — rather than indexing every column just in case."},"follow_up_hint":"Ask them how they'd identify which columns actually need an index in a real slow-query scenario (EXPLAIN plan)."},
  {"id":"idx-0002","question":"What is a composite index, and does column order matter when creating one?","type":"conceptual","difficulty":3,"roles":["backend"],"topics":["dbms","sql"],"tech_keywords":["composite-index"],"companies_common":["Amazon","Google"],"source_style":"faang-common","reference_solution":{"key_points":["A composite (multi-column) index is built on two or more columns together, stored sorted by the first column, then the second within each first-column value, and so on","Column order matters a lot: the index can efficiently satisfy queries filtering on a left-to-right prefix of the indexed columns, but not on the trailing columns alone","Example: an index on (last_name, first_name) can efficiently serve queries filtering on last_name alone, or on both, but NOT efficiently serve a query filtering only on first_name","General guidance: put the column with highest selectivity / most frequently filtered alone first, or match the order queries actually filter in"],"sample_answer":"A composite index spans multiple columns, and internally it's sorted by the first column, then by the second column within each value of the first, and so on — like a phone book sorted by last name, then first name within each last name. Column order matters significantly: the index can efficiently serve a query filtering on the leftmost prefix of the indexed columns — like filtering on just last_name, or on last_name and first_name together — but it can't efficiently serve a query filtering only on first_name, since that's not a usable prefix of the index. When designing a composite index, I'd order columns based on the actual query patterns — typically putting the column that's filtered on its own most often, or that has the highest selectivity, first."},"follow_up_hint":"Ask them to design the right composite index for a table frequently queried by 'WHERE status = ? AND created_at > ?'."},
  {"id":"idx-0003","question":"Explain the difference between a clustered and a non-clustered index.","type":"conceptual","difficulty":3,"roles":["backend"],"topics":["dbms"],"tech_keywords":["clustered-index"],"companies_common":["Amazon","Microsoft"],"source_style":"faang-common","reference_solution":{"key_points":["A clustered index determines the physical storage order of the table's rows on disk — the table data itself is stored sorted by the clustered index key","A table can have only one clustered index (since data can only be physically sorted one way), often the primary key by default","A non-clustered index is a separate structure with pointers back to the actual row location (or the clustered index key), and a table can have many of them","Lookups via a non-clustered index that need columns not in the index require an extra 'lookup' step to fetch the full row (unless it's a 'covering' index that includes all needed columns)"],"sample_answer":"A clustered index actually determines the physical order in which rows are stored on disk — the table's data IS the clustered index, sorted by its key. Because rows can only be physically ordered one way, a table can have only one clustered index, commonly the primary key. A non-clustered index is a separate structure that stores the indexed column's values sorted, along with a pointer back to where the actual row lives (often the clustered index key), and a table can have many non-clustered indexes. Because a non-clustered index doesn't contain the full row, looking up columns not included in that index requires an extra step to fetch the actual row — unless the index is a 'covering' index that includes every column the query needs, avoiding that extra lookup entirely."},"follow_up_hint":"Ask what a 'covering index' is and how it avoids the extra row lookup."},
  {"id":"idx-0004","question":"You have a query that's running slowly despite an index existing on the filtered column. What are possible reasons the database might not be using that index?","type":"scenario_based","difficulty":4,"roles":["backend"],"topics":["dbms","sql"],"tech_keywords":["query-optimization"],"companies_common":["Amazon","Google"],"source_style":"faang-common","reference_solution":{"key_points":["Applying a function or expression to the indexed column in the WHERE clause (e.g. WHERE LOWER(email) = ?) prevents the optimizer from using a plain index on 'email'","Low selectivity: if the column has few distinct values (e.g. a boolean 'is_active'), the optimizer may correctly decide a full scan is cheaper than using the index","Implicit type mismatch/casting between the column and the compared value can prevent index usage","Outdated table statistics can cause the query planner to misjudge costs and pick a suboptimal plan","Mentions using EXPLAIN/EXPLAIN ANALYZE to actually see the chosen query plan rather than guessing"],"sample_answer":"A few common reasons: applying a function to the column in the WHERE clause, like WHERE LOWER(email) = 'x', prevents the database from using a plain index on email unless there's a functional index specifically on LOWER(email). If the column has low selectivity — few distinct values, like a boolean flag — the optimizer might correctly determine a full table scan is actually cheaper than jumping through the index for a large fraction of the table anyway. An implicit type mismatch, like comparing a string column against a number, can also disable index usage since the database has to cast values first. Outdated table statistics can cause the query planner to misjudge the cost and pick a worse plan. The right first step is always to run EXPLAIN or EXPLAIN ANALYZE on the actual query to see the real execution plan being chosen, rather than guessing."},"follow_up_hint":"Ask them what running EXPLAIN ANALYZE would show them, and what specific line in the output would confirm the index isn't being used."}
]

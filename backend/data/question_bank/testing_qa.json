[
  {"id":"test-0001","question":"What is the testing pyramid, and why should unit tests outnumber end-to-end tests?","type":"conceptual","difficulty":1,"roles":["general_sde","backend","frontend"],"topics":["testing"],"tech_keywords":["testing","unit-testing"],"companies_common":["Amazon","Google","Microsoft"],"source_style":"common-interview","reference_solution":{"key_points":["Pyramid shape: many fast unit tests at the base, fewer integration tests in the middle, fewest slow end-to-end tests at the top","Unit tests are fast, cheap, and isolate failures precisely to one unit","End-to-end tests are slow, flaky, and expensive to maintain but catch real cross-system issues","Over-investing in E2E tests leads to slow, brittle test suites that developers start ignoring","Each layer catches different classes of bugs, so none should be skipped entirely"],"sample_answer":"The testing pyramid describes an ideal ratio: many fast, cheap unit tests forming the base, a moderate number of integration tests in the middle, and few slow, expensive end-to-end tests at the top. Unit tests isolate failures precisely and run in milliseconds, so they should catch most logic bugs early. E2E tests are valuable for catching real cross-system issues but are slow and flaky, so an inverted pyramid (mostly E2E) leads to a slow, unreliable suite that teams eventually stop trusting."},"follow_up_hint":"Ask them to identify where a specific bug (e.g. a broken API contract between two services) would be best caught in this pyramid."},
  {"id":"test-0002","question":"Explain the difference between unit tests, integration tests, and end-to-end tests with an example of each.","type":"conceptual","difficulty":1,"roles":["general_sde"],"topics":["testing"],"tech_keywords":["testing"],"companies_common":["Amazon","Infosys","TCS"],"source_style":"common-interview","reference_solution":{"key_points":["Unit tests verify a single function/class in isolation, mocking its dependencies","Integration tests verify multiple components working together, e.g. a service talking to a real test database","End-to-end tests verify a full user flow through the actual running system, e.g. via a browser","Gives a concrete, distinct example for each","Explains the trade-off of speed and confidence increasing/decreasing inversely across the three"],"sample_answer":"A unit test verifies one function in isolation, e.g. testing a calculateDiscount() function with mocked inputs. An integration test verifies components working together, e.g. testing that a repository layer correctly reads and writes to a real (test) database. An end-to-end test verifies an entire user flow through the real running system, e.g. a browser automation test that logs in, adds an item to a cart, and checks out. Each layer trades speed for realism — unit tests are fastest but least realistic, E2E tests are slowest but most realistic."},"follow_up_hint":"Ask what a 'test double' is and the difference between a mock, stub, and fake."},
  {"id":"test-0003","question":"What is Test-Driven Development (TDD), and what is the red-green-refactor cycle?","type":"conceptual","difficulty":2,"roles":["general_sde"],"topics":["testing","tdd"],"tech_keywords":["tdd","testing"],"companies_common":["ThoughtWorks","Amazon","Microsoft"],"source_style":"common-interview","reference_solution":{"key_points":["Write a failing test first (red) for behavior that doesn't exist yet","Write the minimal code to make that test pass (green)","Refactor the code for quality while keeping tests passing","Explains this drives design from the perspective of how the code will be used, since the test is written first","Notes the discipline requires writing only enough code to pass the current test, not anticipating future requirements"],"sample_answer":"TDD is writing a failing test before writing the implementation. The cycle is: red — write a test for behavior that doesn't exist yet, and watch it fail; green — write the minimal code needed to make that test pass, without over-engineering; refactor — clean up the implementation (and test) while keeping all tests green. This forces you to think about the API/interface from the caller's perspective first, since you write the test before the implementation exists."},"follow_up_hint":"Ask them to walk through TDD for implementing a simple function, like FizzBuzz, step by step."},
  {"id":"test-0004","question":"What is the difference between a mock, a stub, and a fake in unit testing?","type":"conceptual","difficulty":2,"roles":["general_sde"],"topics":["testing"],"tech_keywords":["testing","mocking"],"companies_common":["Amazon","Microsoft","Google"],"source_style":"common-interview","reference_solution":{"key_points":["A stub returns canned/predetermined responses to calls, with no verification of how it was called","A mock additionally verifies interactions — that specific methods were called with specific arguments, a specific number of times","A fake is a lightweight but genuinely working implementation, e.g. an in-memory database instead of a real one","Explains overuse of mocks (verifying implementation details) can make tests brittle to refactoring","Gives a concrete example distinguishing all three"],"sample_answer":"A stub just returns a fixed, predetermined response when called, with no assertions on how it was invoked — useful for controlling test inputs. A mock goes further, actually verifying interactions, like asserting a specific method was called exactly once with specific arguments. A fake is a real, working but simplified implementation, like an in-memory list-based repository standing in for a real database. Over-relying on mocks that assert internal implementation details can make tests brittle — they break on refactors that don't actually change behavior."},"follow_up_hint":"Ask when they'd prefer a fake over a mock for testing a repository layer."},
  {"id":"test-0005","question":"How would you test a function that depends on the current time or random number generation?","type":"practical_implementation","difficulty":2,"roles":["general_sde","backend"],"topics":["testing"],"tech_keywords":["testing"],"companies_common":["Amazon","Google"],"source_style":"common-interview","reference_solution":{"key_points":["Inject the time/randomness source as a dependency (a clock or RNG interface) rather than calling system time/random directly inside the function","In tests, substitute a fixed/deterministic clock or seeded RNG","This makes the function's behavior fully deterministic and reproducible in tests","Mentions this is an application of dependency injection specifically for testability","Notes some languages/frameworks provide time-mocking utilities as an alternative when refactoring isn't feasible"],"sample_answer":"The key is to not call the system clock or random number generator directly inside the function — instead, inject a clock or RNG as a dependency (a parameter or injected interface). In production, you pass the real system clock; in tests, you pass a fixed, controllable fake clock or a seeded random generator, making the function's output fully deterministic and reproducible. This is really just dependency injection applied specifically to make time/randomness testable, rather than reaching for a time-mocking library to patch global state, which is more fragile."},"follow_up_hint":"Ask how they'd test code that was NOT originally written with this injectable design, without a large refactor."},
  {"id":"test-0006","question":"What is code coverage, and why is 100% coverage not the same as a bug-free codebase?","type":"conceptual","difficulty":1,"roles":["general_sde"],"topics":["testing"],"tech_keywords":["testing","code-coverage"],"companies_common":["Amazon","Infosys"],"source_style":"common-interview","reference_solution":{"key_points":["Code coverage measures what percentage of code lines/branches were executed by the test suite","100% line coverage only means every line ran at least once — it says nothing about whether the right assertions were made","A test can execute a line without actually verifying its output is correct","Coverage doesn't measure edge cases, ordering issues, or concurrency bugs not exercised by the test's specific inputs","Coverage is a useful signal for finding untested code, not a proof of correctness"],"sample_answer":"Code coverage measures what fraction of the codebase's lines or branches were executed while running the test suite. The catch is that a test can execute a line of code without actually asserting anything meaningful about its behavior — a test with no assertions at all can still achieve 100% coverage while verifying nothing. Coverage also doesn't tell you about untested edge cases, unusual input combinations, or concurrency bugs that just happen not to be triggered by the existing tests' specific inputs. It's a useful tool for spotting completely untested code, not proof that tested code is correct."},"follow_up_hint":"Ask them to give an example of a test with 100% line coverage that still fails to catch a real bug."},
  {"id":"test-0007","question":"Explain what a flaky test is, common causes, and how you'd approach fixing one.","type":"troubleshooting","difficulty":2,"roles":["general_sde"],"topics":["testing"],"tech_keywords":["testing"],"companies_common":["Amazon","Google","Meta"],"source_style":"common-interview","reference_solution":{"key_points":["A flaky test sometimes passes and sometimes fails with no code changes between runs","Common causes: race conditions/timing assumptions, shared mutable state between tests, reliance on real time/network/external services, test execution order dependency","Approach: reproduce reliably (run repeatedly, isolate), identify the non-determinism source, then either fix the root cause or properly isolate/mock it","Explains why simply retrying flaky tests until they pass is a dangerous anti-pattern that hides real bugs","Mentions test isolation (no shared state, fresh fixtures) as a preventive practice"],"sample_answer":"A flaky test passes or fails inconsistently without any code changes, usually due to timing assumptions (waiting a fixed delay instead of waiting for a real condition), shared mutable state leaking between tests, reliance on real network calls or wall-clock time, or dependence on test execution order. I'd first try to reproduce it reliably, often by running it repeatedly or in isolation versus the full suite, to narrow down whether it's timing, ordering, or external dependency related, then fix the actual root cause — replacing a fixed sleep with a proper wait condition, or mocking the external dependency — rather than just retrying failed runs, which just hides a real bug under statistical noise."},"follow_up_hint":"Ask them to describe a specific flaky test scenario involving async code and how they'd fix it."},
  {"id":"test-0008","question":"What is snapshot testing, and what are its pitfalls?","type":"conceptual","difficulty":2,"roles":["frontend"],"topics":["testing"],"tech_keywords":["testing","react","snapshot-testing"],"companies_common":["Meta","Amazon"],"source_style":"common-interview","reference_solution":{"key_points":["Snapshot testing captures a component's rendered output and compares future runs against that saved snapshot","Fails when the output legitimately changes, requiring a review-and-update step ('snapshot update')","Pitfall: developers can blindly approve/update snapshots without reviewing the actual diff, defeating the test's purpose","Pitfall: large, monolithic snapshots make it hard to tell what specifically changed or why it matters","Best suited for catching unintended changes, not for verifying specific behavior — should be paired with targeted assertions"],"sample_answer":"Snapshot testing captures a component's rendered output the first time and saves it; future test runs compare against that saved snapshot and fail if it changed. It's good for catching unintended visual/structural regressions, but its biggest pitfall is that when a snapshot test fails, developers can just blindly run 'update snapshots' without actually reviewing whether the change was intentional, which defeats the whole purpose. Large snapshots also make it hard to tell exactly what changed and why it matters. It works best as a supplement to specific, targeted assertions, not a replacement for them."},"follow_up_hint":"Ask how they'd prevent teammates from blindly approving snapshot updates in code review."},
  {"id":"test-0009","question":"How would you design integration tests for a service that depends on a third-party payment API?","type":"scenario_based","difficulty":3,"roles":["backend"],"topics":["testing"],"tech_keywords":["testing","api"],"companies_common":["Stripe","Amazon","Uber"],"source_style":"common-interview","reference_solution":{"key_points":["Avoid calling the real third-party API in automated tests — it's slow, costs money/rate limits, and depends on external availability","Use the provider's official sandbox/test-mode API if available, which mimics real behavior without real transactions","For unit/most integration tests, mock the API client at the boundary, testing your own code's handling of various responses (success, decline, timeout, malformed response)","Reserve a small number of true end-to-end tests against the sandbox for critical paths, run less frequently (e.g. not on every commit)","Explains testing failure/edge cases (timeouts, 5xx responses, malformed webhooks) is often more valuable than testing the happy path alone"],"sample_answer":"I'd avoid hitting the real third-party API in the regular test suite — it's slow, can incur real costs, and depends on external uptime. Instead, for most unit and integration tests, I'd mock the API client at the boundary and test how my own code handles various responses: success, a card decline, a timeout, a malformed response, a webhook retry. If the provider offers an official sandbox/test mode, I'd reserve a small number of true end-to-end tests against it for critical paths like checkout, but run those less frequently — e.g. in a nightly job rather than on every commit — to avoid slowing down the main pipeline. Testing failure modes tends to be more valuable than just the happy path, since that's where real production incidents happen."},"follow_up_hint":"Ask how they'd test webhook handling specifically, including duplicate/out-of-order delivery."},
  {"id":"test-0010","question":"What is contract testing, and what problem does it solve in a microservices architecture?","type":"conceptual","difficulty":3,"roles":["backend"],"topics":["testing","microservices"],"tech_keywords":["testing","microservices","contract-testing"],"companies_common":["Amazon","Netflix","Uber"],"source_style":"common-interview","reference_solution":{"key_points":["Contract testing verifies that a service's API matches the expectations of its consumers, without needing to spin up the full dependency chain","A consumer defines a 'contract' (expected requests/responses); the provider's tests verify it still satisfies that contract","Solves the problem of full end-to-end integration tests across many microservices being slow, flaky, and hard to run in isolation","Catches breaking API changes before deployment, without needing every consumer service running simultaneously","Names a common tool (e.g. Pact) as an example of this pattern"],"sample_answer":"Contract testing verifies that a service's API actually matches what its consumers expect, without needing to run the full chain of dependent services together. A consumer team defines a contract — the specific requests it makes and responses it expects — and the provider service runs tests against that same contract to verify it still satisfies it, independently of the consumer actually running. This solves the problem of full end-to-end tests across many microservices being slow and flaky, while still catching breaking API changes before they reach production. Pact is a common tool implementing this pattern."},"follow_up_hint":"Ask how contract testing differs from just versioning the API and documenting breaking changes."}
]
